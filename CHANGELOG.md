# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added

- Phase 6 Phase 5 — notification-tap routing:
  - `Scramble/Scramble/Features/Trips/TripsTab.swift` — `path` is now a `@Binding`. A default `.constant(NavigationPath())` keeps existing call sites and tests building unchanged.
  - `Scramble/Scramble/Features/Root/RootView.swift` — owns the `tripsPath: NavigationPath` so a notification-tap consumption can push to the Trips tab's stack regardless of which tab is showing. `consumeActivationRoute()` drains the router's `pendingRoute` (Req 5.1, 5.2): switches the tab to `.trips`, resets the navigation stack to root, and pushes the looked-up `Trip`. Trip lookups that miss (`tripID` no longer on device) clear the route without modifying navigation state (Req 5.4). Observed via `.onChange(of: activationRouter?.pendingRoute)` plus a `.task` drain for cold-launch taps.

- Phase 6 Phase 4 — app-level wiring:
  - `Scramble/Scramble/App/AppDelegate.swift` — `Environment` gains `activationRouter: NotificationRouter` (Phase 6 local-notification delegate) and `notificationsService: NotificationsService` slots alongside the existing `notificationRouter: RemoteNotificationRouter` (Phase 5 silent-push).
  - `Scramble/Scramble/ScrambleApp.swift` — constructs the notification stack via a `makeNotificationsWiring(engine:)` helper (Decision 12). `LocalWriteHook` is now wrapped in a `PendingChangeBroadcaster` fanning out to both `TripSyncEngine` and the notifications service. `UNUserNotificationCenter.delegate` is installed on the local router from `init`. `WindowGroup` gains an `onOpenURL` (Req 5.6) and a `.onChange(of: scenePhase)` bridge that maps `.active`/`.background` transitions into the service's `ScenePhaseTransition` enum (so the service file doesn't import SwiftUI).
  - `Scramble/Scramble/Notifications/NotificationsServiceEnvironmentKey.swift` — `EnvironmentValues.notificationsService` and `.activationRouter` for trip-domain views.
  - `Scramble/Scramble/Info.plist` — registers `scramble://` as a custom URL type (Req 5.6).
  - `Scramble/Scramble/Features/Trips/TripListView.swift` — create-trip onSave closure now calls `requestAuthorizationIfNeeded(forTrip:)` after the rules-engine pass (Req 3.1).
  - `Scramble/Scramble/Sharing/TripDeletion.swift` — accepts an optional `notificationsService` and calls `.tripDeleted(tripID:)` after `commitDeletion` succeeds (Req 4.2). `TripDetailView` threads its environment service through.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — adds the "Notifications are off — open Settings" affordance (Req 3.5) inside the header when `notificationsService.authStatus == .denied`. Tap calls `UIApplication.shared.open(UIApplication.openSettingsURLString)`.

- Phase 6 Phase 7 + 8 — polish (animations + haptics):
  - `Scramble/Scramble/Theme/Animations.swift` — shared `Animation.scrambleStandard` constant (ease-in-out, 0.22 s) so phase toggles and checkbox toggles share the same motion language (Req 7.1, 7.2).
  - `Scramble/Scramble/Features/Trips/AccordionTimeline.swift` — toggle path now uses `.scrambleStandard` inside a single `withAnimation` block.
  - `Scramble/Scramble/Components/TaskRow.swift` + `PackingItemRow.swift` — checkbox-toggle action now wrapped in `withAnimation(.scrambleStandard)` so fill↔outline + row opacity + strikethrough animate atomically. PackingItemRow skip/restore also wrapped.
  - `Scramble/Scramble/Components/PhaseRow.swift` — `TapToToggleModifier` fires a medium-impact haptic on the same view event that initiates expand/collapse (Req 8.2).
  - `Scramble/Scramble/Features/Trips/PackingSheet.swift` — root `.onAppear` fires a soft-impact haptic (Req 8.3).
  - TaskRow / PackingItemRow checkbox + skip/restore already carry light-impact haptics (Req 8.1, 8.4). WhyDisclosure long-press already carries a light-impact haptic at the moment the disclosure becomes visible (Req 8.5).

- Phase 6 Phase 6 — country flag UI:
  - `Scramble/Scramble/Theme/CountryFlag.swift` — `CountryFlag.emoji(for:)` derives the flag emoji from a two-letter ISO 3166-1 alpha-2 code via regional-indicator scalar arithmetic (`U+1F1E6 + (letter - 'A')`). Normalises to uppercase; returns `nil` for `nil`, empty, wrong-length, or non-letter input. ISO-list validation is non-goal — `"XZ"` returns an empty flag rather than rejecting.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — renders the flag to the left of the trip name on the header. `.accessibilityHidden(true)` since the destination is already part of the spoken trip name (Req 9.6).
  - `Scramble/Scramble/Features/Trips/TripDraft.swift` — adds `countryCode: String?` and a `normaliseCountryCode(_:)` helper returning `.clear` / `.set(<uppercase>)` / `.invalid`.
  - `Scramble/Scramble/Features/Trips/TripEditorView.swift` — new "Destination" section with a 2-letter country code field, live flag preview on the trailing edge, inline validation error for non-letter input, and a separate text buffer so transient invalid input (mid-typing) does not overwrite the last valid value.
  - `Scramble/Scramble/Features/Trips/TripPersistence.swift` — propagates `draft.countryCode` to both `create(...)` and `apply(_:to:...)` paths.
  - `Scramble/ScrambleTests/Theme/CountryFlagTests.swift` — table-driven valid alpha-2 mapping + invalid-input nil coverage.
  - `Scramble/ScrambleTests/Features/TripDraftCountryCodeTests.swift` — normalisation cases (empty → clear, two letters → uppercase, anything else → invalid).

- Phase 6 Phase 3 — notification service + router + broadcaster:
  - `Scramble/Scramble/Notifications/NotificationCenterProtocol.swift` — abstraction over `UNUserNotificationCenter` (authorization, settings re-read, add, list pending, remove pending/delivered, setDelegate). Returns `UNAuthorizationStatus` directly so tests don't need to fabricate `UNNotificationSettings`. `UNUserNotificationCenter` conforms via a small extension.
  - `Scramble/Scramble/Notifications/PendingChangeBroadcaster.swift` — multicast wrapper over `PendingChangeNotifier` (Decision 12). Forwards `notifyPendingChanges(...)` to N children in registration order. Strong references — both `TripSyncEngine` and `NotificationsService` are owned for the app's lifetime by `ScrambleApp`.
  - `Scramble/Scramble/Notifications/NotificationRouter.swift` — `UNUserNotificationCenterDelegate` plus a 1-slot `pendingRoute` (Req 5.2). Two callers fill the slot: `didReceive` for taps and `enqueue(_:)` for the debug `scramble://` URL path. Foreground delivery returns `[.banner, .sound]` (Req 1.3). `route(from userInfo:)` and `route(from url:)` parse the two carriers with parity.
  - `Scramble/Scramble/Notifications/NotificationsService.swift` — `@MainActor` orchestrator. `PendingChangeNotifier` conformance triggers a `.localWrite` reschedule on every `LocalWriteHook.commit`. Immediate-flush reasons (`.appActivation`, `.scenePhaseBackground`, `.tripDeleted`, `.authChanged`) bypass the 2 s coalesce window. `requestAuthorizationIfNeeded(forTrip:)` is the in-context auth gate (Req 3.1). `handleScenePhase(.becameActive | .enteredBackground)` runs the backfill / flush. The reconciler is full-fleet — no per-record classification.
  - `Scramble/ScrambleTests/Doubles/StubNotificationCenter.swift` — recording stub used by the service tests. `nextAddError` lets a test force a one-shot throw from `add(_:)`.
  - `Scramble/ScrambleTests/Notifications/PendingChangeBroadcasterTests.swift` — registration-order forwarding + sibling isolation.
  - `Scramble/ScrambleTests/Notifications/StubNotificationCenterTests.swift` — sanity coverage for the recording stub's contract.
  - `Scramble/ScrambleTests/Notifications/NotificationRouterTests.swift` — `consumeRoute` atomicity, last-write-wins, `userInfo` parser, URL parser, malformed-input rejection.
  - `Scramble/ScrambleTests/Notifications/NotificationsServiceTests.swift` — auth gate on insert (gate fires only when plans exist; denied skips re-prompt), coalesce window collapses two `.localWrite` calls into a single reconcile, `.tripDeleted` is immediate, denied→authorized scene flip runs backfill, authorized→denied cancels all activation requests, identical-reconcile no-op detection.

- Phase 6 Phase 2 — notification pure primitives:
  - `Scramble/Scramble/Notifications/NotificationIdentifier.swift` — identifier scheme `scramble.activation.<UUID>.<phase-rawValue>` with `make`, `parse`, and `threadID(for:)`. Activation identifiers are split on `.` into four exact parts; anything else is rejected. Thread identifiers group all of a trip's notifications in Notification Center.
  - `Scramble/Scramble/Notifications/NotificationPlanner.swift` — pure mapping from `[Trip]` + `[UUID: [TripTask]]` + `now` + `calendar` + `cap` to `[ActivationPlan]`. Skips `weeksBefore`, `afterTrip`, compressed `duringTrip`; skips phases whose activation day ≤ today's `startOfDay`; sorts by fire date then `Trip.startDate` then `Trip.id`; clamps to the 60-pending cap (Req 2.1). `body(tripName:phase:outstandingTasks:)` exposed so the reconciler can no-op detect a body match without rerunning the full plan.
  - `Scramble/Scramble/Notifications/NotificationReconciler.swift` — diffs the planner output against `pendingNotificationRequests`. `toAdd` includes plans whose `(tripID, phase)` is missing or whose body differs; `toRemove` includes pending identifiers in the activation namespace that are not in the plan. Pending requests outside the activation namespace are ignored. Order-stable: `toAdd` follows plan order, `toRemove` follows pending order.
  - `Scramble/ScrambleTests/Notifications/NotificationIdentifierTests.swift` — round-trip across every `Phase` case and parser rejection coverage.
  - `Scramble/ScrambleTests/Notifications/NotificationPlannerTests.swift` — eligibility, past-day skip, body text, 60-cap with deterministic tie-break, outstanding-task-count threading, and a property test asserting no plan ever fires on or before today's calendar day.
  - `Scramble/ScrambleTests/Notifications/NotificationReconcilerTests.swift` — empty input, body-match no-op, body-differs add, defensive cleanup of stray activation-namespace identifiers, ignore foreign namespace, and order stability.

- Phase 6 Phase 1 — `SchemaV4` + `Trip.countryCode`:
  - `Scramble/Scramble/Models/Trip.swift` — adds `var countryCode: String?` (ISO 3166-1 alpha-2, uppercase) and a default-`nil` initializer parameter. Per-trip presentation metadata for the destination flag emoji (Decision 5); not a rules-engine input.
  - `Scramble/Scramble/Models/Schema.swift` — introduces `SchemaV4` (versionedSchema with the V3 model list plus the new `Trip.countryCode` column) and registers a `.lightweight` V3 → V4 stage on `AppMigrationPlan`. SwiftData adds the column on first open via automatic inference because the property is `Optional` with a `nil` default.
  - `Scramble/Scramble/Persistence/ModelStore.swift` — both `globals` and `tripsLocal` containers now pin to `SchemaV4`. The `globals` container has no `Trip` rows in production (Phase 5.1 moved them to `tripsLocal`), so the data effect on `globals` is zero; the schema still updates so the `Trip` model type stays consistent across containers.
  - `Scramble/Scramble/Sharing/Translators/TripRecordTranslator.swift` — encodes `countryCode` to the CKRecord using `as CKRecordValue?` so a `nil` assignment clears the field for participants; decode treats a missing field as "leave existing value alone".
  - `Scramble/ScrambleTests/Sharing/Translators/TripRecordTranslatorTests.swift` — four new round-trip cases covering set, unset, set→unset→set toggle, and missing-field ignored.
  - `Scramble/ScrambleTests/Persistence/SchemaV4MigrationTests.swift` — plan-shape coverage plus an on-disk V3-seed → V4-reopen round-trip parametrised across two container shapes; verifies trips survive and `countryCode == nil`.
- `specs/phase-6-notifications-polish/` — Phase 6 spec (requirements.md, design.md, decision_log.md, tasks.md). Local phase-activation notifications via `UNCalendarNotificationTrigger`, deep-link routing through `UNUserNotificationCenterDelegate`, country flag emoji on Trip Detail header via `SchemaV4` lightweight migration adding `Trip.countryCode`, `PendingChangeBroadcaster` over `LocalWriteHook` to multicast change events to both `TripSyncEngine` and a new `NotificationsService`, plus the polish pass (transitions, haptics, VoiceOver labels + accessibility custom action for "Why is this here?", Dynamic Type AX2 reflow). 55 tasks across 11 phases.

### Changed

- `specs/OVERVIEW.md` — marked Phase 5.1 Done and listed Phase 6 as Planned.

### Fixed

- `import os` added to `Scramble/Scramble/App/SignInResumeCoordinator.swift`, `Scramble/Scramble/Components/PackingSummarySection.swift`, `Scramble/Scramble/Features/Trips/PersonLookup.swift`, `Scramble/Scramble/Features/Trips/TripEditorPeoplePicker.swift`, `Scramble/Scramble/Sharing/LocalWriteHook.swift`, `Scramble/Scramble/Sharing/TripSyncEventBus.swift`. These files use `modelLogger.error` with `os` privacy-string interpolation but the build had been failing without the explicit import; the failure was hidden until other compile errors cleared.
- `Scramble/Scramble/App/SignInResumeCoordinator.swift` — `deinit` marked `isolated` so it can read `@MainActor`-isolated stored properties (`inFlight`, `observers`) under Swift 6 strict checking. The previous `deinit` triggered "cannot access property … with a non-Sendable type" once the rest of the build was unblocked.
- `Scramble/ScrambleTests/Sharing/LocalWriteHookPBT.swift` — `static let scenarios` marked `nonisolated` so the `@Test(arguments:)` macro can read it from outside MainActor; the in-place `#Predicate { $0.tripID == trip.id }` was hoisted to a `let tripID = trip.id` capture so the predicate macro receives a `UUID` value, not a `Trip` entity.
- `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorPBT.swift` — `static let crossProduct` marked `nonisolated` for the same `@Test(arguments:)` reason.
- `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorPhase51Tests.swift` — stale references to `Phase.daysBefore`, `ItemSource.rules`, and `TripAttributes.climate = .warm` updated to the current names (`Phase.dayBefore`, `ItemSource.rule`, `TripAttributes.setSingle(.weather, value: "warm")`).
- `Scramble/ScrambleTests/Persistence/SchemaV3MigrationTests.swift` — plan-shape assertion updated to expect three stages (V1→V2, V2→V3, V3→V4) and four schemas in `AppMigrationPlan`.

- Phase 5.1 — PR #6 review-fixer pass 6 (round-6 P1 items):
  - `Scramble/Scramble/Features/Trips/TripEditorPeoplePicker.swift` — after a successful `Person` delete, the picker now invokes `SnapshotMaintenance.sweep` against `tripsLocal` and commits through `LocalWriteHook.commit` so any non-roster snapshots with no remaining packing-item referrers are swept in the same session. Denormalised snapshots are intentionally preserved per Decision 7; this only catches the orphan-with-no-referrer subset that would otherwise wait for next warm-launch.
  - `Scramble/Scramble/Sharing/LocalWriteHook.swift` — `commitChanges` now wraps `applyZoneChange` in per-zone do/catch. A flag-update failure on one zone no longer skips the remaining zones, and the notifier loop excludes failed zones so the engine never receives a "changes ready" signal for a zone whose `TripZoneState.pendingUploadFlags` weren't updated. Errors are logged with the zone name.

- Phase 5.1 — PR #6 review-fixer pass 5:
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — `deleteTrip`'s catch now calls `modelContext.rollback()` before surfacing the toast. `TripDeletion.delete` stages every record-delete and zone-state delete in the context before calling `hook.commitDeletion`; a thrown commit otherwise leaves staged-but-unsaved deletes pending, which makes the trip vanish from `@Query` results until the next app launch even though the disk still has the data.
  - `Scramble/Scramble/Features/Trips/PackingItemForm.swift` — `performAdd` now guards against `trip.participantSnapshots` containing no entry for the picker-selected Person (mid-roster-removal race). The previous code silently created an ownerless `TripPackingItem` that would render incorrectly across every device. New `PackingItemFormError.missingPersonSnapshot` is thrown so the form's inline-error path surfaces the failure.
  - `Scramble/Scramble/Features/Trips/TripEditorPeoplePicker.swift` — `performDelete`'s save-failure catch now logs via `modelLogger.error` AND shows a "Couldn't delete this person — try again." alert. Previously the catch silently rolled back and restored optimistic state with zero feedback.
  - `Scramble/Scramble/App/SignInResumeCoordinator.swift` — observer hop replaced `MainActor.assumeIsolated` with `Task { @MainActor in ... }`. `OperationQueue.main` is not formally the MainActor's executor; if CloudKit changes the delivery queue or Swift 6 strict-checking tightens the assumeIsolated trap, the silent trap would be a regression hazard. The unconditional Task wrap is safe regardless of delivery queue.

- Phase 5.1 — PR #6 review-fixer pass 4:
  - `Scramble/Scramble/Sharing/TripSyncEventBus.swift` — `subscribeOrchestrator` / `subscribeCoordinator` doc comments now state the dispatch-order contract per-method: orchestrator runs first, coordinator runs second, both receive the same event even if the orchestrator handler throws after committing a partial write. The file header had the contract; per-method docs make it visible at the registration call site.
  - `Scramble/Scramble/Sharing/SnapshotMaintenance.swift` — `handlePackingItemDeletion` now traps in DEBUG when called against an item already staged for deletion (`context.deletedModelsArray` contains it). The doc comment described the ordering contract but nothing enforced it; the in-context referrer count would silently miscount and orphan-delete a snapshot if a caller ran `context.delete(item)` first.

- Phase 5.1 — PR #6 review-fixer pass 3:
  - `Scramble/Scramble/Features/Trips/TripPersistence.swift` — `apply` now `throws` and propagates errors from `SnapshotMaintenance.handleRosterRemoval` instead of logging-and-continuing. The routine interleaves a fetch with mutations, so a silent partial pass could leave one snapshot flipped to `isRosterMember=false` without the corresponding delete sweep. Caller (`TripDetailView` save handler) already wraps the call in do/catch with `modelContext.rollback()`, so the propagated error rolls back cleanly.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — wraps the `TripPersistence.apply` call in the existing do/catch so a roster-removal fetch failure rolls back the edit and returns `false` to the editor.
  - `Scramble/ScrambleTests/Features/Trips/TripPersistenceTests.swift` — added `try` to the two `apply` call sites.
  - `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `leaveShare` doc comment now explicitly states the known limitation: when the server delete fails with a tolerable error, the local `TripZoneState` is gone and no `CKSyncEngine` retry is queued. The owner's Participants section will continue to show this participant until the share is revoked or re-accepted. Acceptable for the family-app context; a future "leave-pending" sentinel could close the gap.
  - `specs/phase-5.1-wire-trip-crud-tripslocal/tasks.md` — cleaned up corrupt `Blocked-by` field on task 25 (generation artefact with `"routing, through, routing, through, ..."` repeated 45 times).

- Phase 5.1 — PR #6 review-fixer pass 2:
  - `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — `deleteFromGlobals` now defensively sweeps any `TripZoneState` rows for the trip from `globals` before the save. Production code never inserts there (all four `TripZoneState(...)` call sites bind `tripsLocal`), but the defence catches a hypothetical orphan from a future translator change or a foreign sync source.
  - `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorPhase51Tests.swift` — new `deleteFromGlobalsRemovesOrphanZoneStates` test seeds a `TripZoneState` directly in `globals` and verifies it's swept on relocation.
  - `Scramble/Scramble/Sharing/SnapshotMaintenance.swift` — `handlePackingItemDeletion` doc now states the ordering contract explicitly: callers must invoke the routine BEFORE `context.delete(item)` so the in-context referrer count reflects the still-present item. Production callers already do this; the contract was only documented in test comments.
  - `Scramble/Scramble/Components/PackingSummarySection.swift` — replaces N `PersonLookup` fetches per render with a single `TripPersistence.resolveParticipants` batch query over all roster `personID`s. The `[UUID: Person]` dictionary is looked up per `ForEach` iteration in O(1).
  - `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `leaveShare` now tolerates `.networkUnavailable` / `.networkFailure` / `.serverRejectedRequest` in addition to `.zoneNotFound`. Local cleanup proceeds in all four cases (the user's intent is "remove this trip from my device"); the next sync reconciles the shared-DB membership. New `isTolerableLeaveError` static helper centralises the predicate.
  - `Scramble/Scramble/Features/Trips/PersonLookup.swift` — replaces silent `try?` with `do/catch` + `modelLogger.error`. Schema-mismatch / store-corruption errors now surface in Console instead of degrading invisibly to "person not found".

- Phase 5.1 — PR #6 review-fixer pass:
  - `Scramble/Scramble/Components/PackingSummarySection.swift` — `handleOpen` no longer silently no-ops when `PersonLookup` can't resolve a snapshot's `personID`. The row's button is now disabled when the underlying `Person` is missing (stale snapshot, in-progress migration), giving sighted and VoiceOver users a visible signal that the row isn't actionable. If a tap races the resolution the handler emits a `modelLogger.error` so Console surfaces the gap.
  - `Scramble/Scramble/App/SignInResumeCoordinator.swift` — triggers that arrived during the trailing replay were being dropped: the in-flight task unconditionally cleared `pendingReplay` on exit, after the trigger had set it but before `inFlight = nil` allowed a new task to start. The task now captures the flag once after the trailing replay, clears the state, and re-enters `runResumeIfNeeded` if a late trigger landed. Each round still bounds itself to ≤1 trailing replay; sustained storms chain rounds.
  - `Scramble/ScrambleTests/App/SignInResumeCoordinatorTests.swift` — new `lateTriggerStartsNewRound` test locks the late-trigger behaviour in (asserts `delta == 2`: one trailing replay + one late-trigger round).
  - `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — `enqueueAll` sorts the union of trip IDs by `uuidString` before iterating. Set iteration order is non-deterministic; sorting makes journal-insertion order stable across runs (easier logs, easier PBT failure traces).
  - `Scramble/Scramble/Sharing/TripSyncEventBus.swift` — header comment documents the handler-atomicity contract: dispatch order is fixed orchestrator-then-coordinator, so if the orchestrator throws mid-rule-engine-run the coordinator still receives the same event. Today both handlers commit through atomic `ModelContext.save()` paths, so a partial predecessor state is structurally impossible — future subscribers must add their own catch-and-rollback or accept the partial-state risk.

- Phase 5.1 — pre-push-review cleanup of the Phase 5 work:
  - `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — `globalsContext` / `tripsLocalContext` are now `private`; new `journalCount() throws -> Int` exposes the back-stop count without leaking the context. Steps 12 and 13 of `startOrResume` are merged — the in-memory `expectedRecordNames` set already covers every reachable snapshot, so the defensive second fetch is dropped. `deleteFromGlobals` now uses the explicit `packingItems → tasks → participantSnapshots → trip` reverse-cascade order to match the iOS 26.4 workaround documented in `TripDeletion`. `enqueueAll` uses `Set.union` instead of an explicit seen-set. New `zoneName(for:)` + `ownerZoneID(for:)` static helpers centralise the canonical zone naming.
  - `Scramble/Scramble/Sharing/LocalWriteHook.swift`, `Scramble/Scramble/Sharing/Translators/TripZoneStateRecordTranslator.swift`, `Scramble/Scramble/Sharing/UITestSharingService.swift` — call `ZoneMigrationCoordinator.ownerZoneID(for:)` / `zoneName(for:)` instead of inlining `"trip-\(uuid)"`.
  - `Scramble/Scramble/RulesEngine/RulesEngineTriggerOrchestrator.swift`, `Scramble/Scramble/Sharing/Translators/TripZoneStateRecordTranslator.swift` — drop the two duplicate private `parseTripID(from:)` helpers; both call sites now route through `ZoneMigrationCoordinator.parseTripID`.
  - `Scramble/Scramble/App/SignInResumeCoordinator.swift` — `deinit` removes the two `NotificationCenter` observers it installs in `start()`; production lifetime is forever but the cleanup matters for tests that create+drop coordinators.
  - `Scramble/Scramble/ScrambleApp.swift` — switches from `migrationCoordinator.globalsContext.fetchCount(...)` to `migrationCoordinator.journalCount()` for the back-stop visibility log.
  - `Scramble/ScrambleTests/App/SignInResumeCoordinatorTests.swift` — storm-collapse test rewritten with a `ResumeGate` checked-continuation gate so the inflight run is sequenced deterministically; `delta == 1` assertion now holds without scheduling-jitter races.
  - `CLAUDE.md` — project status updated to mention Phase 5.1 (container topology, chokepoint extension, 15-step migration coordinator, event bus, sign-in resume) so future agent sessions inherit the right context.
  - `specs/phase-5.1-wire-trip-crud-tripslocal/decision_log.md` — adds Decision 7: `SignInResumeCoordinator.init` drops the unused `CKContainer` parameter; `migrationCoordinator.isCloudAvailable()` is the single source of truth for the availability check.

### Added

- Phase 5.1 — migration coordinator + engine event multicast + sign-in resume (tasks 29–39):
  - `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — `startOrResume` rewritten as the 15-step algorithm: `.completed` is a terminal no-op, four-quadrant existence branch over `(tripsLocal, globals)` decides whether to relocate and/or delete, step 10 clears stale `sentRecordNames` + `zoneSaved` before re-marking the journal `.stageBInProgress` so prior-aborted-run events do not contaminate the resume, retroactively dirty-flags every Stage A `TripPersonSnapshot` for the trip (Req 4.9), and the two `ModelContext.save()` calls are sequential `@MainActor` invocations with no `await` between so the engine cannot interleave. New `relocateToTripsLocal(tripID:)` helper (private) copies every persisted field — `id`, `masterItemID`, `currentlyMatchesRules`, `pinnedByUser`, `userDeletedOnThisTrip`, `state`, `isCompleted`, `assigneePersonID`, `name`, `ckRecordSystemFields`, every Codable-blob field — preserving `personSnapshot` references. `enqueueAll` now scans both containers so pre-Phase-5.1 trips still in `globals` get a journal entry.
  - `Scramble/Scramble/Sharing/TripSyncEngine.swift` — adds `.zoneSaved`, `.recordsSaved`, `.recordsFailed` cases to `TripSyncEvent`. `handleSentChanges` emits `.recordsSaved` for `event.savedRecords` and `.recordsFailed` for `event.failedRecordSaves`. New `handleSentDatabaseChanges` emits one `.zoneSaved` per confirmed zone-save event so the coordinator can mark the journal's `zoneSaved` flag.
  - `Scramble/Scramble/Sharing/TripSyncEventBus.swift` — owns the single iteration of `syncEngine.events` and dispatches to two named subscribers (orchestrator + coordinator). Handlers are throwing closures; each dispatch is wrapped in `do/catch` so a throwing handler is logged via `modelLogger.error` and the bus continues for the other subscriber. Late or duplicate registration trips `assertionFailure` in DEBUG, logs `fault` in release. Test-only `stop()` cancels the iteration task. Convenience overloads wire `RulesEngineTriggerOrchestrator` and `ZoneMigrationCoordinator` directly.
  - `Scramble/Scramble/App/SignInResumeCoordinator.swift` — observes `CKAccountChanged` and `UIScene.didActivateNotification`. `start()` installs the observers and performs an immediate `isCloudAvailable()` re-check (handles "account became available before observer installed"). `runResumeIfNeeded` uses an `inFlight: Task<Void, Never>?` plus `pendingReplay: Bool` flag to collapse storm-fire to exactly one trailing replay regardless of trigger volume. Production init takes a `ZoneMigrationCoordinator`; test init takes the two closures directly so tests can drive the collapse logic without a real coordinator.
  - `Scramble/Scramble/ScrambleApp.swift` — constructs `TripSyncEventBus` and `SignInResumeCoordinator` in `init`; registers orchestrator + coordinator subscribers BEFORE `bus.start()` so no events are missed. `prepareLaunch` starts the bus, then the sign-in resume coordinator (which performs the initial enqueue + Stage B through its single in-flight pipeline), logs a `warning` if `MigrationJournalEntry` count exceeds 100 (back-stop visibility), and finally calls `syncEngine.start()`. Drops the previous direct `for await event in engine.events` loop — the bus owns iteration now.
  - `Scramble/Scramble/RulesEngine/RulesEngineTriggerOrchestrator.swift` — `handle(event:)` switch updated to acknowledge the three new event cases (no-op for the orchestrator; the coordinator subscriber handles them).
  - `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorTests.swift` — rewritten to use two distinct in-memory containers (`globals` + `tripsLocal`) so the four-quadrant algorithm behaves like production. New `makeSetup()` / `coordinatorOnly(from:)` helpers; new "signed-out launch defers Stage B then resumes on sign-in" test (Req 4.7/4.8).
  - `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorPhase51Tests.swift` — new suite for Phase 5.1-specific coverage: `.completed` terminal no-op (step 2), four-quadrant branch (`(none, some)`, `(some, some)`, `(some, none)`, `(none, none)`), relocation preserves every persisted field (Req 4.2), Stage A snapshot retroactive dirty-flagging (Req 4.9), step-10 clear of `sentRecordNames` + `zoneSaved` on resume.
  - `Scramble/ScrambleTests/Persistence/ZoneMigrationCoordinatorPBT.swift` — Swift Testing `@Test(arguments:)` PBT over the cross-product of `InterruptionPoint ∈ {fresh, afterStep10Clear, afterTripsLocalSave, afterGlobalsDelete, afterCompletion}` × `resumeCount ∈ {1, 2, 5}`. Asserts cross-store consistency ([C2](specs/phase-5.1-wire-trip-crud-tripslocal/requirements.md#C2)) and idempotence ([Req 4.6](specs/phase-5.1-wire-trip-crud-tripslocal/requirements.md#4.6)): terminal state is exactly one of (trip in tripsLocal only) or (trip in globals only, `.completed` only); no duplicate journal entries.
  - `Scramble/ScrambleTests/Sharing/LocalWriteHookPBT.swift` — PBT for `commitDeletion` mixed-zone partition. Cross-product over `(vanishingZoneCount, survivingZoneCount, deletedPerVanishing, deletedPerSurviving, changedPerSurviving)` asserts surviving-zone `TripZoneState.pendingUploadFlags` carries only surviving-zone deltas and the notifier received the union of all deleted record IDs across both partitions.
  - `Scramble/ScrambleTests/Sharing/TripSyncEventBusTests.swift` — covers multicast to both subscribers, throwing-subscriber isolation (a handler that throws is logged via `modelLogger.error` and the other subscriber still receives the event), `stop()` cancels the iteration task.
  - `Scramble/ScrambleTests/App/SignInResumeCoordinatorTests.swift` — covers immediate run on `start()` when available; short-circuit when unavailable; `CKAccountChanged` triggers a run; `UIScene.didActivateNotification` fallback triggers a run; storm-fire collapses to exactly one trailing replay (`delta == 1` strict, not `≤ 2`).

- Phase 5.1 — write-path migration (tasks 20–28):
  - `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `createShare`, `leaveShare`, and the lazy `fetchZoneState` save now route through `LocalWriteHook.commit(_:)`. `deleteOwnedTrip` and the private `cleanupLocalState` are deleted (`TripDeletion.delete` covers their behaviour). `leaveShare` tolerates `CKError.zoneNotFound` from the shared-DB delete (covers the case where the owner revoked the share concurrently) and then calls `TripDeletion.delete(tripID:in:hook:zoneDeleter: nil)` for the local cascade. Constructor gains a `hook: LocalWriteHook` parameter.
  - `Scramble/Scramble/Sharing/SharingService.swift` — protocol drops `deleteOwnedTrip(forTrip:)`; `TripDeletion.delete(tripID:in:hook:zoneDeleter:)` is the single Trip-domain deletion entry point.
  - `Scramble/Scramble/Sharing/UITestSharingService.swift` / `Scramble/ScrambleTests/Sharing/FakeSharingService.swift` — drop the protocol's old method to match.

- Phase 5.1 — read-path conversion (tasks 12–19):
  - `Scramble/Scramble/Features/Trips/TripEditorPeoplePicker.swift` — extracted cross-container people picker. Wrapper applies `.modelContainer(globalsContainer)` to a child `PickerContent`; the inner `@Query<Person>` then resolves against globals from inside a Trip Editor that is rooted in `tripsLocal`. The inline `PersonEditor` sheet inherits the picker's container, so its writes go to globals without extra plumbing. Person-delete moved here (annotated `// LocalWriteHookContract: allow — globals context, not tripsLocal`).
  - `Scramble/ScrambleTests/Components/TaskRowAssigneeTests.swift` — covers `TaskRow.assigneeSnapshot(for:)` static helper: returns the snapshot whose `personID` matches `task.assigneePersonID`; nil for missing match, nil for nil assignee, nil for trip with no snapshots.
  - `Scramble/ScrambleTests/Features/Trips/TripPersistenceTests.swift` — covers the new `create(from:in:globals:)` and `apply(_:to:in:globals:)` signatures: create inserts `Trip` + `TripZoneState` + `TripPersonSnapshot` per participant; orphan IDs returned; apply produces the add/update/remove diff against existing snapshots; apply preserves snapshots referenced by packing items as `isRosterMember = false`.
  - `Scramble/ScrambleUITests/TripEditorPickerReactivityUITests.swift` — proves the picker's `@Query` is live against globals: creating a Person from the inline editor appears in the picker without leaving the Trip Editor.

- Phase 5.1 — chokepoint extension (tasks 6–11):
  - `Scramble/Scramble/Sharing/LocalWriteHook.swift` — adds `commitDeletion(_ context: ModelContext, zoneIDsBeingDeleted: Set<CKRecordZone.ID>)`. Partitions pending changes by mapped-zone membership in the vanishing-zone set: surviving-zone records follow the regular `commit(_:)` path (flag update + notifier signal); vanishing-zone records skip the per-`TripZoneState` flag update but still signal the notifier with deleted IDs so the engine queues `deleteRecord` operations alongside `deleteZone`. Refactors `commit` and `commitDeletion` to share a private `commitChanges(in:zoneIDsBeingDeleted:)` path so the only behavioural difference is the flag-write guard.
  - `Scramble/ScrambleTests/Sharing/LocalWriteHookTests.swift` — three new tests for the mixed-zone partition contract: (a) deletion in vanishing zone Z1 + edit in surviving zone Z2 produces notifier-deleted-from-Z1 and dirty-in-Z2; (b) nil-mapping rows (`TripZoneState` in `deletedModelsArray`) are invisible to both flag update and notifier; (c) all-vanishing-zone deletions produce notifier signals but leave the about-to-be-deleted `pendingUploadFlags` blob untouched. `RecordingNotifier.calls` is now publicly mutable so tests can reset between commits in the same suite.

- Phase 5.1 — foundation (tasks 1–5):
  - `Scramble/Scramble/Persistence/GlobalsContainerKey.swift` — SwiftUI environment carrier for the `globals` `ModelContainer`, mirroring `TripsLocalContainerKey`. Default value is `ModelStore.containers.globals`; production injects in `ScrambleApp.rootContent()` so Trip subtrees rooted in `tripsLocal` can re-root cross-container reads (people picker, `PersonLookup`).
  - `Scramble/Scramble/Persistence/LocalWriteHookEnvironmentKey.swift` — SwiftUI environment carrier for the shared `LocalWriteHook`. Default value is a `LocalWriteHook` whose notifier is a private `FallbackPendingChangeNotifier` that discards silently in test/UI-test/preview surroundings and emits a `fault`-level log plus DEBUG `assertionFailure` in production. Pairs with the (Phase 6) source-scan contract test to close both halves of the silent-failure mode Phase 5.1 exists to eliminate; documented as decision_log Decision 6.
  - `Scramble/Scramble/Features/Trips/PersonLookup.swift` — `@MainActor enum` with `person(for: UUID, in: ModelContext) -> Person?`. One-shot UUID lookup against a supplied globals context; no observation, no side effects on the context. Used by trip-domain views that need the live `Person` row rather than the snapshot.
  - `Scramble/ScrambleTests/Features/Trips/PersonLookupTests.swift` — tests covering resolution against an in-memory globals container, nil for missing UUID, and no-side-effects on the context.

- `specs/OVERVIEW.md` — catalogue of all feature specs with status, creation date, one-line summary, and per-spec file links. Generated by the `specs-overview` skill; excludes `bugfixes/` and non-doc artifacts.
- `specs/phase-5.1-wire-trip-crud-tripslocal/` — full spec for Phase 5.1 (Wire Trip CRUD through `tripsLocal`): `requirements.md` (EARS-format, 10 clusters + 4 named invariants C1–C4), `design.md` (container topology, dual-entry chokepoint, 15-step relocation algorithm, `TripSyncEventBus`, `SignInResumeCoordinator`, two contract tests), `decision_log.md` (5 decisions), and `tasks.md` (52 tasks across 7 phases with TDD pairing and `blocked_by` dependencies). Closes the wiring gap from Phase 5 that left the SwiftUI view layer bound to `globals` despite the sharing pipeline being ready in `tripsLocal`.
- `Scramble/Scramble/Sharing/SharingService.swift` — new `deleteOwnedTrip(forTrip:)` protocol method (Req 1.4) so the Trip Detail delete handler can tear down the trip's `TripZoneState` and queue the `CKRecordZone` deletion on the private engine alongside the local `Trip` delete. `CloudKitSharingService`, `FakeSharingService`, and `UITestSharingService` all implement it.
- `Scramble/Scramble/Features/Trips/TripDetailView.swift` — participant-mode action menu (Req 6.5) that surfaces a "Leave Share" affordance and calls `SharingService.leaveShare(forTrip:)`. Trip Detail dismisses on success; failures surface via the existing toast helper. Adds a second `confirmationDialog` describing the eventual-consistency behaviour.
- `specs/phase-5-cloudkit-sharing/implementation.md` — three-level explanation (beginner / intermediate / expert) of Phase 5's CloudKit sharing pipeline plus a Completeness Assessment mapping the 13 requirement clusters to fully / partially / missing.

### Changed

- Phase 5.1 — write-path migration callers (tasks 21, 23, 25, 28):
  - `Scramble/Scramble/Features/Trips/PackingSheet.swift` — both `PackingSheet` and the inner `PackingItemGroup` read `@Environment(\.localWriteHook)`; the `save(_:)` helper calls `hook.commit(modelContext)` in place of `try modelContext.save()`.
  - `Scramble/Scramble/Features/Trips/TaskForm.swift` — task add/edit saves through the hook.
  - `Scramble/Scramble/Features/Trips/PackingItemForm.swift` — `performAdd` and `performEdit` now take a `hook: LocalWriteHook` parameter; both call `hook.commit(context)` instead of `context.save()`. The form reads `@Environment(\.localWriteHook)` and passes it to the helpers.
  - `Scramble/Scramble/Features/Trips/TripListView.swift` / `TripDetailView.swift` — trip create / edit saves go through the hook. Both views inject `\.globalsContainer` into `TripPersistence.create` / `apply` and pass the hook + globals context to the post-save `RulesEngineRunner` so the engine reaches both stores.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — owner-side `deleteTrip` replaces the previous `modelContext.delete(trip) + save + sharingService.deleteOwnedTrip(...)` sequence with a single `TripDeletion.delete(tripID:in:hook:zoneDeleter:)` call (zoneDeleter derived from the production `CloudKitSharingService.syncEngine`).
  - `Scramble/Scramble/Features/MasterLists/MasterTaskEditor.swift` / `MasterPackingEditor.swift` — `runEngineAndDismiss` constructs the runner with `context: tripsLocalContainer.mainContext` (trips) and `mastersContext: modelContext` (globals, where the master rules live).
  - `Scramble/Scramble/Features/Root/RootView.swift` — scene-phase warm-pass runs the engine against tripsLocal + globals via the env-injected hook and globals container.
  - `Scramble/Scramble/RulesEngine/RulesEngineRunner.swift` — adds `mastersContext: ModelContext?` and `hook: LocalWriteHook?` parameters. `mastersContext` defaults to `context` (back-compat for single-container tests); the runner reads master snapshots from `mastersContext` and writes plan output to `context` via `apply(plan:context:hook:)`. `hook` defaults to a private `NoopRunnerNotifier`-backed instance so unit tests work without sync wiring.
  - `Scramble/Scramble/RulesEngine/Apply.swift` — `apply(plan:context:hook:)` calls `hook.commit(context)` instead of `context.save()`. `insertAddedPacking` now writes the V3 `personSnapshot` relationship — it looks up the snapshot from `trip.participantSnapshots` by `master.personID` and drops the V2 `Person` fetch entirely (which would have failed against `tripsLocal` anyway). Rule-driven inserts for personIDs without a corresponding snapshot on the trip are skipped with a log line.
  - `Scramble/Scramble/ScrambleApp.swift` — constructs `LocalWriteHook` first so `makeSharingService` can inject it into `CloudKitSharingService`. `makeTriggerOrchestrator` and `runColdLaunchEnginePass` thread `mastersContext: ModelStore.containers.globals.mainContext` and the production hook through the engine runner.
  - `Scramble/ScrambleTests/RulesEngine/ApplyTests.swift` / `RulesEngineRunnerTests.swift` / `PackingFormSaveTests.swift` — every call site updated for the new `apply(plan:context:hook:)` and `performAdd(...,hook:)` / `performEdit(...,hook:)` signatures. Tests that exercise `insertAddedPacking` / `runForTrip` seed a `TripPersonSnapshot` on the trip so the new snapshot-based packing-item insert finds an owner; assertions on `inserted.person?.id` switch to `inserted.personSnapshot?.personID`.

- Phase 5.1 — read-path conversion (tasks 13, 15, 16, 19):
  - `Scramble/Scramble/Components/TaskRow.swift` — adds the static `assigneeSnapshot(for:)` helper that reads `task.trip?.participantSnapshots?.first { $0.personID == task.assigneePersonID }`. Avatar render switched from the V2 `Person` traversal to the snapshot's `name` + `colourID`.
  - `Scramble/Scramble/Components/PackingSummarySection.swift` — sorts roster snapshots from `trip.participantSnapshots` instead of the V2 `trip.participants`. The row hands a `TripPersonSnapshot` to `PackingSummaryRow`; the sheet-open callback re-resolves to a `Person` via `PersonLookup` against globals so the downstream `PackingSheet` API is unchanged.
  - `Scramble/Scramble/Features/Trips/TaskForm.swift` — assignee picker reads `mode.trip?.participantSnapshots`; the picker selects a `personID` and the form writes `task.assigneePersonID`.
  - `Scramble/Scramble/Features/Trips/PackingItemForm.swift` — `performAdd` looks up the snapshot from `trip.participantSnapshots` by `person.id` and writes the V3 `personSnapshot` relationship instead of the V2 `person → Person`.
  - `Scramble/Scramble/Features/Trips/PackingSheet.swift` — `participantIDSignature` derives from `trip.participantSnapshots`.
  - `Scramble/Scramble/Features/Trips/TripDraft.swift` — `init(from: Trip)` populates `participantIDs` from `trip.participantSnapshots` (filtered to roster members).
  - `Scramble/Scramble/Features/Trips/TripListView.swift` — row avatar strip reads `trip.participantSnapshots`. `TripEditorView` save handler passes the globals context to `TripPersistence.create`.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — header avatar strip + handlePackingSheetDismiss read `trip.participantSnapshots`. `TripEditorView` save handler passes globals context to `TripPersistence.apply`.
  - `Scramble/Scramble/Features/Trips/TripEditorView.swift` — drops the inline people picker; mounts `TripEditorPeoplePicker` instead. Removes Person-related @State, the @Query<Person>, and the per-Person delete handlers (they live in `PickerContent` now).
  - `Scramble/Scramble/Features/Trips/TripPersistence.swift` — `create(from:in:globals:)` and `apply(_:to:in:globals:)`: resolves participants from globals, inserts `Trip` + `TripZoneState` + `TripPersonSnapshot` per participant on create (Req 1.5), diffs `participantSnapshots` on apply (add/update/remove via `SnapshotMaintenance.handleRosterRemoval`), and stops writing the V2 `trip.participants` relationship (constraint C3).
  - `Scramble/Scramble/Sharing/LocalWriteHook.swift` — `commitDeletion` partitions by `zoneName` (owner-agnostic) rather than full `CKRecordZone.ID`. The hook synthesises zone IDs with `CKCurrentUserDefaultName`, but a participant trip's `TripZoneState` carries the remote owner's name; matching by full ID would misclassify those zones as surviving and trigger a re-insert of the just-deleted `TripZoneState` row inside `applyZoneChange`. Caught by the `participantSideCleanupWithoutZoneDeletion` test under the new TripDeletion.delete signature.
  - `Scramble/ScrambleTests/Features/Trips/PackingFormSaveTests.swift` — seed now creates a `TripPersonSnapshot` instead of writing the V2 `trip.participants` set; `addInsertsWithDocumentedDefaults` asserts `inserted.personSnapshot?.personID` instead of `inserted.person?.id`.

- Phase 5.1 — chokepoint extension callers (tasks 9, 11):
  - `Scramble/Scramble/Sharing/SnapshotMaintenance.swift` — every routine (`propagatePersonEdit`, `handleRosterRemoval`, `handlePackingItemDeletion`, `sweep`) is now mutate-only: no `context.save()` calls, no `flagDirty` helper (deleted). The caller is responsible for committing through `LocalWriteHook.commit(_:)` so one user action collapses to one commit and the hook handles all dirty-marking uniformly.
  - `Scramble/Scramble/Sharing/TripDeletion.swift` — `delete` signature is now `delete(tripID:in:hook:zoneDeleter:)`. The single context save routes through `hook.commitDeletion(context, zoneIDsBeingDeleted:)` so deleted record IDs reach the engine while the per-`TripZoneState` flag write is skipped for the vanishing zone. Drops the leading `pendingUploadFlags = Data()` reset — the new `commitDeletion` partition makes it redundant.
  - `Scramble/ScrambleTests/Sharing/SnapshotMaintenanceTests.swift` — every test now drives the routine, commits via `LocalWriteHook.commit(_:)`, and (where relevant) asserts the recording notifier observed the expected dirty/deleted record IDs.
  - `Scramble/ScrambleTests/Sharing/TripDeletionTests.swift` — every existing test instantiates a `LocalWriteHook` and passes it to `TripDeletion.delete`. Adds three new tests: `commitDeletion` routing yields a single vanishing-zone notifier signal carrying every deleted record ID; participant-scope deletion does not enqueue a zone delete; deleting a non-existent trip is idempotent.

- Phase 5.1 — container topology wiring (task 3):
  - `Scramble/Scramble/Features/Root/RootView.swift` — Trips tab subtree now binds `.modelContainer(tripsLocalContainer)`; scene-phase warm-pass switched from `@Environment(\.modelContext)` (globals) to the env-injected `\.tripsLocalContainer.mainContext` so the rules engine runs against the container that now holds the trip-domain rows.
  - `Scramble/Scramble/ScrambleApp.swift` — constructs the shared `LocalWriteHook(notifier: engine)` once at launch and injects `\.globalsContainer`, `\.tripsLocalContainer`, and `\.localWriteHook` from `rootContent()` so the entire tree resolves cross-container reads and the per-commit dirty-marking chokepoint consistently. `runColdLaunchEnginePass` switched from `ModelStore.shared.mainContext` (globals) to `ModelStore.containers.tripsLocal.mainContext`.

- `Scramble/Scramble/Features/Trips/TripDetailView.swift` — owner trip delete now calls `SharingService.deleteOwnedTrip(forTrip:)` after the local delete completes, fixing the missing zone deletion path (Req 1.4). Share toolbar visibility now treats a `nil` `ownerIdentity` as ownership so brand-new trips (no `TripZoneState` yet) show the Share affordance per Req 5.1; participant trips always carry an `.otherUser` zone state so they remain excluded.
- `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `makeShareParticipant` now returns "Loading…" instead of "Invited participant" for pending invitations whose display name and email are still being fetched (Req 7.8). The Req 7.1 terminal fallback ("Invited participant") is preserved for non-pending states.
- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — three CloudKit-hot-path efficiency cleanups: (1) `apply(deletedRecordIDs:)` uses predicate-scoped fetches per record type instead of a `_ in true` fetch over every model; (2) `scope(for:)` parses the trip ID out of the zone name and predicate-scopes the `TripZoneState` lookup; (3) `cacheSentSystemFields` switches on `record.recordType` so only the matching model class is fetched.
- `CLAUDE.md` — Project status updated to reflect Phase 5 landing (CloudKit sharing infra, `SchemaV3`, dual containers, `TripSyncEngine`, `CloudKitSharingService`, Share toolbar + Participants section, silent-push routing, release-prep checklist).

- Phase 5 — UI + release-prep (tasks 30–36):
  - `Scramble/Scramble/Features/Trips/ShareToolbarButton.swift` — Trip Detail trailing toolbar item. Visible only when the current user owns the trip's zone (`SharingService.ownerIdentity == .currentUser`). Calls `createShare(forTrip:)` and mounts `UICloudSharingControllerRepresentable` in a sheet; errors surface via a `TransientToast`.
  - `Scramble/Scramble/Features/Trips/ParticipantsSection.swift` — Trip Detail Participants section between the chip row and the timeline. Async-loads `[ShareParticipant]` from the sharing service; renders pending vs accepted via an inline acceptance label; owner-side rows wrap a manage-button labeled `"Manage <displayName>"` and tap-presents `UICloudSharingController`; participant-side rows are plain read-only content (Req 7.4). Pending-name placeholder updates without user interaction once the name resolves (Req 7.8).
  - `Scramble/Scramble/Features/Trips/MigrationRetryBanner.swift` — Inline banner above the Trip List Active section, surfacing every `.failed` `MigrationJournalEntry` via a `@Query` (Req 4.4). Tap calls back into `ZoneMigrationCoordinator.retry(tripID:)` through the `\.zoneMigrationCoordinator` environment key. Banner section short-circuits when no failed rows exist so the list layout stays unchanged in the steady state.
  - `Scramble/Scramble/Features/Trips/TripListView.swift` — Adds two new `@Query<MigrationJournalEntry>` instances (in-progress + failed). Lifts the `inProgressJournals → syncingTripIDs` mapping out of `TripRow` so the per-row badge becomes a simple `Set.contains` check, which is more deterministic than the per-row `@Query` against a composite predicate. `TripRow` gains `isSyncing: Bool` and renders a blue "Syncing…" capsule badge with `accessibilityIdentifier("tripRow.syncingBadge")` when true (Req 4.8).
  - `Scramble/Scramble/Sharing/RulesLastEvaluatedTracker.swift` — `@Observable` per-trip timestamp tracker (Req 8.8). `RulesEngineTriggerOrchestrator` writes via `record(tripID:at:)` on each non-self-originated `.zoneChanged` event; `TripDetailView` reads via `time(forTrip:)` when `sharingService.ownerIdentity` reports `.otherUser` and renders the "Rules last evaluated {relative-time}" subline using `RelativeDateTimeFormatter`. Omitted on owner-viewed trips.
  - `Scramble/Scramble/Sharing/UITestSharingService.swift` — DEBUG-only `SharingService` stub used in UI-test mode. Returns hard-coded ownership + participant lists from static dictionaries populated by `UITestSeed`. `ScrambleApp.init` swaps this in when `EnvironmentProbe.isUITestHost`; production builds continue to use `CloudKitSharingService`.
  - `Scramble/Scramble/Persistence/RulesLastEvaluatedTrackerEnvironmentKey.swift`, `ParticipantViewingEnvironmentKey.swift`, `MigrationCoordinatorEnvironmentKey.swift` — three small environment keys shared between `ScrambleApp` and the new view code (`\.rulesLastEvaluatedTracker`, `\.isParticipantViewingSharedTrip`, `\.zoneMigrationCoordinator`).
  - `Scramble/Scramble/Explainability/WhyResolver.swift` — both `reason(for:context:)` overloads gain a `hideOnUnresolvedMaster: Bool = false` parameter and return `WhyDisclosure.Reason?` (Req 3.3). When `true` and the rule-driven item's master cannot be found, the resolver returns `nil` so the WhyDisclosure affordance is omitted from the layout. Owner-viewed call sites pass `false` and continue to receive `.ruleMasterDeleted` as before.
  - `Scramble/Scramble/Components/TaskRow.swift` and `Scramble/Scramble/Components/PackingItemRow.swift` — both read `\.isParticipantViewingSharedTrip` from the environment and forward it as `hideOnUnresolvedMaster:` when calling `WhyResolver.reason`. The existing `resolvedReason: Reason?` cache already short-circuits the inline `WhyDisclosureView` when the resolver returns `nil`.
  - `Scramble/Scramble/Features/Trips/TripDetailView.swift` — wires the new surfaces: computes `isOwnerOfShared` / `isParticipantOnShared` from `sharingService?.ownerIdentity(forTrip:)`; mounts `ShareToolbarButton` (owner only) and `ParticipantsSection` (any share); injects `\.isParticipantViewingSharedTrip` for descendant rows; appends the participant-only "Rules last evaluated" subline.
  - `Scramble/Scramble/RulesEngine/RulesEngineTriggerOrchestrator.swift` — gains an optional `tracker: RulesLastEvaluatedTracker` and `now: () -> Date` so each non-self-originated `.zoneChanged` event also records a timestamp before invoking `run(tripID:)`. Default initialiser keeps backwards compatibility for Phase 5 sync-infrastructure call sites.
  - `Scramble/Scramble/ScrambleApp.swift` — selects the sharing service per environment (`UITestSharingService` for UI-test mode, `CloudKitSharingService` otherwise); constructs a single `RulesLastEvaluatedTracker` and threads it through the orchestrator + view environment; gates `enqueueAll() + runStageB() + syncEngine.start()` on `isHeadless == false` so UI-test fixtures don't get their seeded `.stageBInProgress` journal entries flipped to `.failed` by a coordinator looking at an empty `tripsLocal`. `MigrationGate` content moves into a `@ViewBuilder` helper to keep `body` linter-clean.
  - `Scramble/Scramble/Persistence/UITestSeed.swift` — new `applyIfRequested(globalsContainer:tripsLocalContainer:)` dual-container entry point. Phase 5 fixtures (`phase5-shared-trip-owner`, `phase5-shared-trip-participant`, `phase5-migration-states`) seed `Trip` rows into globals (where `@Query<Trip>` reads from until Stage B), `TripZoneState` rows into tripsLocal (where the sharing service reads from), and populate `UITestSharingService.{ownerIdentitiesByTrip, participantsByTrip}`. `UITestSharingService.reset()` runs at the top of every fixture apply so state never leaks across launches.
  - `docs/release-prep.md` — release-prep checklist gated by Req 13: promote new CloudKit record types and zone topology introduced by `SchemaV3` from Development to Production via the CloudKit Dashboard before TestFlight or App Store submission.
  - Tests: `ScrambleTests/Explainability/WhyResolverParticipantHideTests.swift` (owner vs participant resolution, manual items always render), `ScrambleTests/Sharing/RulesLastEvaluatedTrackerTests.swift` (tracker writes, orchestrator wiring including self-origination filtering and foreign-zone skipping, `tripID(fromZoneName:)` round-trip), `ScrambleUITests/Phase5SharingUITests.swift` (Share toolbar visibility owner vs participant, Participants pending-vs-accepted distinction, display-name fallback chain `display → email → fallback`, participant-side read-only, owner-side `Manage <name>` tap target), `ScrambleUITests/Phase5MigrationStateUITests.swift` (retry banner appears for `.failed` journal entries; `.stageBInProgress` trips render the "Syncing…" badge).
  - `docs/agent-notes/phase-5-ui-surfaces.md` — load-bearing decisions for the new UI layer (owner-check synchrony, participant-side WhyDisclosure hide via the environment flag, MigrationGate skip in test branches, Phase 5 fixture dual-container layout, `UITestSharingService.reset()` discipline).

### Fixed

- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — `apply(fetchedRecords:)` previously aborted the whole batch when a single translator threw, leaving the records that had already mutated the context uncommitted until the next save quietly picked them up. `CKSyncEngine` considers an event "delivered" once `handleEvent` returns, so the failed record could never be re-processed. Each record is now wrapped in its own `do/catch` that logs via `modelLogger` and continues; the surviving translations are saved once at the end of the batch (PR #5 / BUG-04).
- `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `acceptShare` returned `metadata.ownerIdentity.userRecordID?.recordName` (an opaque `_abc123…` string) as `AcceptedShareResult.ownerDisplayName`. UI surfaces would render the record ID directly. Replaced with the Req 7.1 fallback chain: `PersonNameComponentsFormatter.string(from:)` → `lookupInfo.emailAddress` → `nil` (PR #5 / BUG-06 / SEC-01).
- `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `makeShareParticipant` computed `isCurrentUser` as `participant.role == .owner`, which only ever matched the trip's owner. From a participant's device the owner row was flagged as the current user and the participant's own row was not. Now uses `CKShare.currentUserParticipant.userIdentity.userRecordID` as the source of truth so each device's own row is identified correctly (PR #5 / BUG-08).
- `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `personNameDisplayName` previously allocated a fresh `PersonNameComponentsFormatter` per call; participants and the new owner-name flow now share a `private static let personNameFormatter`.
- `Scramble/Scramble/App/RemoteNotificationRouter.swift` — `TripSyncEngineNotificationFetcher.fetchChanges(scope:)` returned successfully when the requested `CKSyncEngine` instance was `nil` (pre-`start()` launch path), so `RemoteNotificationRouter.route` returned `.newData` for a fetch that never happened. UIKit then marked the notification handled and never re-delivered it. The fetcher now throws `NotificationFetcherError.engineUnavailable` so `route` returns `.failed` (PR #5 / BUG-10).
- `Scramble/Scramble/Sharing/UICloudSharingControllerRepresentable.swift` — `cloudSharingControllerDidSaveShare` was a no-op while `cloudSharingControllerDidStopSharing` called `onDismiss`. The SwiftUI parent's `.sheet(isPresented:)` binding therefore stayed `true` after a successful invite send because UIKit's internal dismissal didn't reach SwiftUI state. Both delegate methods now call `onDismiss` (PR #5 / BUG-13).
- `Scramble/Scramble/Sharing/Translators/TripPackingItemRecordTranslator.swift` — the `from(_:into:)` decode previously cleared `item.personSnapshot` when the inbound record's `personSnapshotID` key resolved to `nil`. Because `CKRecord[key] == nil` is true for both "absent" and "explicitly cleared", an older-schema record (or a partial update) would destroy the local relationship. Now only assigns when the field resolves to a UUID, matching the `tripID` handling immediately above (PR #5 / BUG-14).
- `Scramble/Scramble/Sharing/TripSyncEngine.swift` + `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `createShare` added the share's record ID to the engine's pending changes, but the engine's record provider only knew how to encode Trip / TripTask / TripPackingItem / TripPersonSnapshot via the translator dispatch — the `CKShare` itself was never returned, so the share record was silently not uploaded and invitees could not receive the invitation. `TripSyncEngine` now exposes `enqueueShareSave(_:)` which caches the `CKShare` instance keyed by recordID; the engine's record provider returns the cached share, and `handleSentChanges` drops the cache entry once CloudKit confirms the save (PR #5 / BUG-02).
- `Scramble/Scramble/Persistence/Migrations/SchemaV3MigrationStage.swift` — Stage A's `backfillSnapshots` inserted `TripPersonSnapshot` rows and set `snapshot.trip = trip`, but the `Trip.participantSnapshots ↔ TripPersonSnapshot.trip` relationship is intentionally unpaired (no `inverse:`, see the V3 cascade-traversal workaround) so SwiftData cannot auto-populate the trip-side array. Every existing install would see an empty `trip.participantSnapshots` after migration, breaking the Participants section and snapshot-maintenance paths. The migration now explicitly assigns `trip.participantSnapshots = existingSnapshots + newSnapshots` (PR #5 / new bot review).
- `Scramble/Scramble/Models/Schema.swift` — `MigrationJournalEntry.isStageBComplete` previously required `!expectedRecordNames.isEmpty`, so a trip with zero records could never complete Stage B even after the zone-save event fired. The journal stayed in `.stageBInProgress` indefinitely. The guard is gone; `Set.isSubset(of:)` already returns `true` for empty sets so the completion correlation works for both empty and non-empty record sets (PR #5 / new bot review).
- `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — added a comment on `enqueueAll` documenting why the source fetch deliberately stays on `tripsLocalContext` (switching it to `globalsContext` without Phase 5.1's record-relocation step queues journals that get stuck in `.stageBInProgress` forever).
- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — `apply(deletedRecordIDs:)` previously relied on SwiftData cascade for trip-deletion fan-out, but `Trip.participantSnapshots` uses `.nullify` (V3 cascade-traversal workaround). Remote-deleting a trip therefore left orphaned `TripPersonSnapshot` rows. The method now walks each trip's snapshots and deletes them explicitly before deleting the trip (PR #5 / iteration-3 bot review).
- `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — `acceptShare` previously called `sharedEngine?.fetchChanges()` with optional-chaining; if the share-acceptance arrived before `MigrationGate` released and `start()` had run, the explicit fetch was silently dropped. Now guards on a non-nil `sharedEngine` and logs the deferral; `automaticallySync = true` means the engine picks up the new zone once it starts. Owner-display-name resolution extracted to `resolveOwnerDisplayName(from:)` so both the engine-ready and engine-deferred paths return the same fallback chain (PR #5 / iteration-3 bot review).
- `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — `fetchJournal(tripID:)` previously used `try?` and returned `nil` indistinguishably for "no journal exists" and "fetch failed". Now logs via `modelLogger.error` on failure so masked migration stalls are visible in diagnostics (PR #5 / iteration-4 bot review).

- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — `handleFetchedChanges` reduced self-origination via `zoneRecordIDs.allSatisfy { wasSelfOriginated($0) }`, which short-circuits on the first `false`. Because `wasSelfOriginated` removes consumed entries, any self-originated IDs that followed a non-self ID in the same event were left in `sentRecordIDs` indefinitely, slowly leaking the echo-guard set. Now maps every ID through `wasSelfOriginated` before reducing so every entry is consumed exactly once per event.
- `Scramble/Scramble/Models/Schema.swift` — `MigrationJournalEntry.expectedRecordNames` and `sentRecordNames` previously did inline `try? JSONDecoder().decode(...) ?? []`, silently dropping corrupt blobs without a log line. Both bridges now route through `CodableBridge.decode` / `.encode` so decode failures log via `modelLogger` and degrade to defaults consistently with the other Codable bridges in the schema.
- Pre-push review identified that `LocalWriteHook`, `SnapshotMaintenance`, `TripDeletion.delete`, and Stage B's record-relocation step are all code-complete and unit-tested but have no production call sites yet (Trip CRUD still routes through the `globals` container). Tracked as Phase 5.1 in `docs/implementation-phases.md` rather than silently deferred.
- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — `apply(deletedRecordIDs:)` and `cacheSentSystemFields` previously ran up to four sequential `FetchDescriptor` lookups per record (one per trip-zone entity type). Both methods now bucket IDs by record type once and run one fetch per bucket with `ids.contains`, cutting the per-batch query count from O(4·N) to O(4) for the delete path and O(types-touched) for the sent-fields path.
- `Scramble/Scramble/Sharing/TripSyncEngine.swift` — `loadStateBlob(for:)` and `persistState(_:scope:)` no longer allocate fresh `JSONDecoder` / `JSONEncoder` instances per call; both now use `static let` encoders hoisted onto the type. The state-update path fires on every engine event, so per-call allocation was unnecessary churn.
- `Scramble/Scramble/Sharing/PendingUploadFlags.swift` — `decode(_:)` and `encode()` now use `static let` `JSONEncoder` / `JSONDecoder` instances. The chokepoint round-trips through these every time `LocalWriteHook` is invoked.
- `Scramble/Scramble/Models/Codable/CodableBridge.swift` — `decode` and `encode` now use `static let` `JSONEncoder` / `JSONDecoder` instances. Every blob read/write on `@Model` Codable bridges (`Trip.attributes`, `MasterTaskItem.conditions`, `MasterPackingItem.conditions`, the new `MigrationJournalEntry` set bridges) runs through here on hot paths (rules-engine evaluation, translators, UI render).
- `Scramble/Scramble/Features/Trips/TripDetailView.swift` — the "Rules last evaluated …" subline previously allocated a `RelativeDateTimeFormatter` on every body invocation. Now uses a `private static let` formatter so participant-side trips don't churn allocations on every re-render.
- `docs/implementation-phases.md` — new Phase 5.1 entry ("Wire Trip CRUD through `tripsLocal`") covering the SwiftUI environment-binding flip, Stage B record relocation between containers, enforcement of `LocalWriteHook` at every Trip-feature save site, wiring of `SnapshotMaintenance` / `TripDeletion`, engine→coordinator event forwarding, the "Network required to share" offline UX (Req 11.2), and integration tests across the env-binding boundary. Ordering-rationale section updated to explain why 5.1 sits between Phase 5 and Phase 6.

- `Scramble/Scramble/App/AppDelegate.swift` — `databaseScope(from:)` previously read `CKNotification.databaseScope`, which only exists on the concrete `CKDatabaseNotification` / `CKRecordZoneNotification` / `CKQueryNotification` subclasses; the build failed with "value of type 'CKNotification' has no member 'databaseScope'". The helper now walks the subclass hierarchy and returns `.public` (treated as "unhandled") when the notification is missing or unrecognised.
- `Scramble/ScrambleTests/Sharing/RemoteNotificationRoutingTests.swift` — added the missing `import UIKit` so `UIBackgroundFetchResult.newData` / `.noData` / `.failed` are visible to the assertions.

- Phase 5 — Stage B + engine + lifecycle (tasks 18–29):
  - `Scramble/Scramble/Persistence/Migrations/ZoneMigrationCoordinator.swift` — Stage B driver. `enqueueAll()` inserts `.pending` `MigrationJournalEntry` rows for trips without a `TripZoneState`; `runStageB()` transitions them to `.stageBInProgress`, inserts the `TripZoneState`, captures the expected record-name set on the journal, marks every record dirty in `TripZoneState.pendingUploadFlags`, and signals the supplied `ZoneMigrationDriver` to save the zone + queue uploads. Event handlers (`handleZoneSaved`, `handleRecordsSaved`, `handleRecordsFailed`) drive each entry to a terminal state via the design's completion correlation. `retry(tripID:)` re-runs `.failed` entries. Skipped when `isCloudAvailable()` returns false (Req 11.3). `TripSyncEngineZoneMigrationDriver` wraps `privateEngine.state.add(...)` for production wiring.
  - `Scramble/Scramble/RulesEngine/RulesEngineRunner.swift` — gained the `ownerIdentity: (UUID) -> OwnerIdentity?` closure. `runForTrip` and `runForAllNonPastTrips` skip trips owned by `.otherUser`; `.currentUser` and `nil` both run (the latter so Phase 1 legacy trips without a `TripZoneState` continue to receive engine runs). Default closure preserves backwards compatibility.
  - `Scramble/Scramble/RulesEngine/RulesEngineTriggerOrchestrator.swift` — bridges `TripSyncEngine.events` to the rules engine. `.zoneChanged` events with `isSelfOriginated == true` are dropped (echo guard); remote changes are routed to `RulesEngineRunner.runForTrip` for the matching trip via the supplied `run` closure.
  - `Scramble/Scramble/Sharing/SnapshotMaintenance.swift` — owner-side `TripPersonSnapshot` lifecycle. `propagatePersonEdit` fans a `Person` rename/colour change out to every snapshot referencing the person and dirty-flags the affected `TripZoneState` for upload (gated by ownership). `handleRosterRemoval` flips `isRosterMember = false` on the matching snapshot and deletes when no packing items still reference it. `handlePackingItemDeletion` removes non-roster snapshots when their last referring item is gone. `sweep` is a defence-in-depth periodic cleanup for orphaned non-roster snapshots. Referrer counting walks `TripPackingItem` directly because the snapshot ↔ packing-item inverse was dropped in V3 to avoid the SwiftData cascade panic.
  - `Scramble/Scramble/Sharing/TripDeletion.swift` — owner-side trip deletion (Req 1.4). `delete(tripID:in:zoneDeleter:)` runs the reverse cascade (`pendingUploadFlags → packing items → tasks → snapshots → trip → TripZoneState`) in one transaction. Owner-scope deletes also queue `deleteZone` via `TripSyncEngineZoneDeleter`; participant-side leaves pass `zoneDeleter: nil` because `CloudKitSharingService.leaveShare` already deletes the shared-DB zone.
  - `Scramble/Scramble/App/AppDelegate.swift` — `UIApplicationDelegate` adapter. `userDidAcceptCloudKitShareWith` forwards to `SharingService.acceptShare`; `didReceiveRemoteNotification:fetchCompletionHandler:` parses `CKNotification.databaseScope` and dispatches to `RemoteNotificationRouter`. Both dependencies are pulled from `AppDelegate.environment`, which `ScrambleApp.init` sets at launch.
  - `Scramble/Scramble/App/RemoteNotificationRouter.swift` — silent-push fan-out (Reqs 9.1, 9.3). Switches on `CKDatabase.Scope` and invokes the matching engine's `fetchChanges()` via the injected `RemoteNotificationFetcher`. Returns `UIBackgroundFetchResult` for the UIKit completion handler. Production fetcher (`TripSyncEngineNotificationFetcher`) wraps `TripSyncEngine.{private,shared}Engine.fetchChanges()`.
  - `Scramble/Scramble/App/MigrationGate.swift` — launch-blocking splash. `prepare` closure runs `enqueueAll() + runStageB()`, starts the sync engine (skipped in test/UI-test/preview branches), and attaches the trigger orchestrator to `TripSyncEngine.events`. Mounts the wrapped content once `prepare` returns.
  - `Scramble/Scramble/Persistence/SharingServiceEnvironmentKey.swift` — `\.sharingService` environment carrier (Optional; `nil` for previews and the test/UI-test branches).
  - `Scramble/Scramble/ScrambleApp.swift` — full Phase 5 wiring. Constructs `TripSyncEngine`, `CloudKitSharingService`, `ZoneMigrationCoordinator`, `RemoteNotificationRouter`, and the trigger orchestrator in `init`; sets `AppDelegate.environment`; wraps `RootView` in `MigrationGate`; injects `\.sharingService` into the view environment. Adds `@UIApplicationDelegateAdaptor(AppDelegate.self)`. The cold-launch `RulesEngineRunner` now passes `service.ownerIdentity(forTrip:)` so participant trips are skipped from the very first pass.
  - `Models/Schema.swift` — `MigrationJournalEntry` gained `expectedRecordNamesData`, `sentRecordNamesData`, and `zoneSavedFlag` (all Optional so SwiftData's column inference is safe). New `MigrationStageState` enum (`pending`, `stageBInProgress`, `completed`, `failed`) and bridge properties (`state`, `expectedRecordNames`, `sentRecordNames`, `zoneSaved`, `isStageBComplete`).
  - Tests: `Persistence/ZoneMigrationCoordinatorTests.swift` (happy path, ownership/signed-out gates, resume after kill, retry, parameterised PBT scenarios for idempotence, convergence over failure/retry sequences, and resume totality across every journal state), `RulesEngine/RulesEngineOwnershipGateTests.swift` (engine no-ops for `.otherUser`, runs for `.currentUser`/`nil`, fan-out filters by ownership, orchestrator drops self-originated events and ignores malformed zone names), `Sharing/SnapshotMaintenanceTests.swift` (every cleanup trigger plus the ownership gate), `Sharing/TripFlagSyncTests.swift` (`pinnedByUser` / `userDeletedOnThisTrip` round-trip via translators; engine preserves pinned items even when their rules no longer match), `Sharing/TripDeletionTests.swift` (reverse-cascade ordering, zone deletion enqueued on owner-side only, idempotent against a missing trip, participant-side cleanup with `zoneDeleter: nil`), `Sharing/RemoteNotificationRoutingTests.swift` (`.private` / `.shared` / `.public` and fetcher-failure paths return the right `UIBackgroundFetchResult`).
  - `docs/agent-notes/sync-infrastructure.md` updated with the Stage B / lifecycle layer and the new conventions (`MigrationJournalEntry.state` as canonical surface; event-handler-only state advancement; `TripDeletion.delete` as the canonical owner-side removal path).

- Phase 5 — Sync infrastructure (tasks 6–17):
  - `Scramble/Scramble/Sharing/SharingService.swift` — injectable seam. `SharingService` protocol + `AcceptedShareResult`, `ShareParticipant`, `OwnerIdentity`. `ownerIdentity(forTrip:)` is synchronous per Req 10.4 and reads only from `TripZoneState`.
  - `Scramble/Scramble/Sharing/CloudKitSharingService.swift` — production impl. `createShare` builds `CKShare(recordZoneID:)`, hands it to `privateEngine.state.add(pendingRecordZoneChanges:)`, caches `TripZoneState.shareID`. `acceptShare` calls `CKContainer.accept`, queues a `saveZone` on the shared engine, triggers `fetchChanges()`. `leaveShare` deletes the shared zone then runs local cleanup in reverse-cascade order (`pendingUploadFlags → packing items → tasks → snapshots → trip → zone state`). `participants` applies Req 7.1's `displayName → email → "Invited participant"` fallback chain. `ownerIdentity(forTrip:)` returns `.currentUser` when the cached `TripZoneState.zoneOwnerName == CKCurrentUserDefaultName` (or scope is private), else `.otherUser(displayName:)`.
  - `Scramble/Scramble/Sharing/UICloudSharingControllerRepresentable.swift` — SwiftUI `UIViewControllerRepresentable` wrapping `UICloudSharingController`. Constructed with the `CKShare` and `CKContainer`; surfaces `onDismiss` and `onSaveFailure` callbacks so Trip Detail can show non-blocking error alerts per Req 11.4.
  - `Scramble/Scramble/Sharing/TripSyncEngine.swift` — façade over two `CKSyncEngine` instances (private + shared databases). Conforms to `CKSyncEngineDelegate`; `nextRecordZoneChangeBatch` builds records from pending IDs via the translators; `handleEvent` routes `fetchedRecordZoneChanges` to per-translator `from(_:into:)` calls and re-caches `ckRecordSystemFields` on `sentRecordZoneChanges`. Pure-Swift surface (`buildBatch(scope:pendingRecordIDs:)`, `apply(fetchedRecords:)`, `apply(deletedRecordIDs:)`, `markSelfOriginated(_:)`, `wasSelfOriginated(_:)`) is testable without a real `CKSyncEngine`. The class also conforms to `PendingChangeNotifier` so `LocalWriteHook` can forward `LocalWriteHook.commit` notifications.
  - `Scramble/Scramble/Sharing/TripSyncStateStore.swift` — protocol + `FileTripSyncStateStore` (writes to `~/Library/Application Support/Scramble/CKSync/{private,shared}.state`, flags `isExcludedFromBackup`) and `InMemoryTripSyncStateStore` (test variant with `returnCorruptDataForScopes` knob for the corruption-recovery path). `TripSyncEngine.loadStateBlob(for:)` logs decode failures, clears the corrupt blob, and returns `nil` so the engine reconstructs fresh and queues a full reconciliation.
  - `Scramble/Scramble/Sharing/LocalWriteHook.swift` — single dirty-marking chokepoint for `tripsLocal` saves (design § "Single dirty-marking chokepoint"). `commit(_:)` inspects `context.insertedModelsArray` / `changedModelsArray` / `deletedModelsArray`, ORs dirty/deleted record names into the matching `TripZoneState.pendingUploadFlags`, calls `context.save` exactly once, then notifies the engine via `PendingChangeNotifier`. Skips models with no resolvable `tripID`. Lint rule / code-review contract: direct `context.save()` on `tripsLocal` outside this hook is forbidden.
  - `Scramble/Scramble/Sharing/PendingUploadFlags.swift` — Codable struct `{dirtyRecordNames: Set<String>, deletedRecordNames: Set<String>}` stored as JSON in `TripZoneState.pendingUploadFlags`. Set form chosen over a bitset because records per trip are low-tens and entity counts grow. `markDirty(recordName:)` and `markDeleted(recordName:)` clear the opposite set so a record never appears in both.
  - `Scramble/Scramble/Sharing/Translators/RecordRepresentable.swift` — protocol contract + `TranslatorError.{blobTooLarge, recordTypeMismatch, missingRequiredRelationship}` + `kRecordBlobSizeCap` (256 KB per Codable blob field).
  - `Scramble/Scramble/Sharing/Translators/{Trip,TripTask,TripPackingItem,TripPersonSnapshot}RecordTranslator.swift` — one translator per entity. Relationships encoded as UUID-valued record fields (e.g., `personSnapshotID: String`), never `CKRecord.Reference`, to avoid cross-record dependency ordering inside `CKSyncEngine` batches. `toRecord(_:in:existing:)` preserves CKRecord system fields by reusing the supplied `existing` record; `from(_:into:)` re-encodes the fetched record's system fields back into the entity's `ckRecordSystemFields` blob via the file-scope `encodeSystemFields(of:)` / `decodeSystemFields(from:)` helpers on `TripRecordTranslator.swift`. Missing record fields decode to the Swift default. `Trip.attributesData` exceeding 256 KB throws `TranslatorError.blobTooLarge`.
  - `Scramble/Scramble/Sharing/Translators/TripZoneStateRecordTranslator.swift` — `CKShare ↔ TripZoneState` updater (the share record is what `TripZoneState` mirrors). Parses `trip-{uuid}` zone names; inserts a placeholder `TripZoneState` on the participant side when a share arrives ahead of any trip record. `zoneID(for:)` exposes the canonical zone-naming convention.
  - `Scramble/Scramble/Persistence/ModelStore.swift` — dual-container API. `ModelStore.containers.globals` keeps SwiftData's CloudKit mirror against the private database; `ModelStore.containers.tripsLocal` is local-only (`cloudKitDatabase: .none`), with CloudKit sync driven by `TripSyncEngine` (Decision 13). Both schemas remain full `SchemaV3` because the deprecated V2 cross-references (`Trip.participants → Person`) survive until V4 cleanup; the split is enforced by convention — globals context holds globals records, tripsLocal context holds trip-zone records. `ModelStore.shared` aliases `containers.globals` for backward compatibility. Production `tripsLocal` store URL lives at `<AppSupport>/Scramble/TripsLocal.store`. Tests get in-memory variants of both via `EnvironmentProbe.isTest` / `isUITestHost` / `isPreview`.
  - `Scramble/Scramble/Persistence/TripsLocalContainerKey.swift` — SwiftUI environment carrier (`\.tripsLocalContainer`) for the second container. `ScrambleApp` mounts globals via SwiftUI's `.modelContainer(_:)` modifier and tripsLocal via `.environment(\.tripsLocalContainer, …)`.
  - `Scramble/ScrambleTests/Sharing/FakeSharingService.swift` — in-process two-side fake `SharingService` for tests (design § "FakeSharingService design"). Two endpoints (`.owner` and `.participant`) share a `FakeSharingBus`; imperative test hooks: `simulateOwnerWrite(_:tripID:)` publishes `.zoneChanged` to the participant side; `simulateAcceptance(forTrip:participantOwnerName:)` publishes `.shareAccepted`; `triggerZoneChange(zoneID:target:)` publishes an empty refetch event; `simulateError(_:)` injects a one-shot error for the next throwing call; `deliveryDelay` adds an async delay for timing-sensitive tests. Lives in the test target only — production code never imports it.
  - Tests: `Sharing/Translators/*Tests.swift` (one suite per translator — UUID-relationship rule, system-fields round-trip, blob size cap, missing-field defaults, recordType mismatch), `Sharing/LocalWriteHookTests.swift` (insert/change/delete dirty bits, single save per commit, notifier wiring, empty commit no-op), `Sharing/FakeSharingServiceTests.swift` (bus delivery, delivery delay, share creation, error injection, owner-only enforcement), `Sharing/CloudKitSharingServiceTests.swift` (share lifecycle contract via `FakeSharingService` — create, simulateAcceptance, leave, participant fallback chain, owner-identity sync), `Sharing/TripSyncEngineTests.swift` (translator dispatch, fetched record application, self-origination consume-once, state corruption recovery, `PendingChangeNotifier` self-origination wiring).
  - `docs/agent-notes/sync-infrastructure.md` — load-bearing decisions for the new sync layer.

- Phase 5 — `SchemaV3` and Stage A migration:
  - `Scramble/Scramble/Models/Schema.swift` adds `SchemaV3` with three new `@Model` entities (`TripPersonSnapshot`, `TripZoneState`, `MigrationJournalEntry`) and three additive top-level fields on existing entities (`Trip.participantSnapshots`/`tripZoneID`/`ckRecordSystemFields`, `TripPackingItem.personSnapshot`/`ckRecordSystemFields`, `TripTask.ckRecordSystemFields`). V2 fields (`Trip.participants`, `TripPackingItem.person`) remain populated and will be removed in V4 per the design's additive-then-deprecated policy.
  - `AppMigrationPlan` extends to a two-stage plan: lightweight V1 → V2 retained, plus a `.custom` V2 → V3 stage whose `didMigrate` closure invokes `SchemaV3MigrationStage.backfillSnapshots(in:)`.
  - `Scramble/Scramble/Persistence/Migrations/SchemaV3MigrationStage.swift` — Stage A backfill. For every `Trip` with `participants` populated, inserts one `TripPersonSnapshot` per `Person` (with `name`, `colourID`, `initialSource`, `isRosterMember = true`) and links each `TripPackingItem.personSnapshot` to the matching trip-scoped snapshot. Idempotent (skips persons that already have a snapshot on the trip; preserves existing packing-item links). Offline-safe — no CloudKit calls; runs unconditionally including when the user is signed out (Req 11.3). `nonisolated` so SwiftData's synchronous `didMigrate` closure can invoke it.
  - `Scramble/Scramble/Persistence/ModelStore.swift` advances both `configuration(probe:)` and `makeContainer(probe:)` to `Schema(versionedSchema: SchemaV3.self)` so production launches (and tests via `ModelStore.shared`) open the V3 schema.
  - Tests: `SchemaV3MigrationTests` (plan-shape: V1/V2/V3 listed, two stages declared, V3 default-value behaviour, new-entity presence, `TripTask` entity-name stability under V3) and `SchemaV3MigrationStageTests` (backfill semantics: snapshot-per-roster-person, `Trip.participantSnapshots` population, `TripPackingItem.personSnapshot` linking, deprecated-V2-field preservation, idempotence on repeated runs, empty-roster and nil-person edge cases). Both suites operate against a V3 in-memory container — V2 → V3 round-trip migration is intentionally not exercised because two `@Model` types named `Trip`/`TripTask`/`TripPackingItem` cannot coexist in the same test process; this matches the `SchemaV2MigrationTests` deferral pattern from Phase 3.

### Changed

- `TripTask` is now a single top-level `@Model` shared by `SchemaV2` and `SchemaV3`. The original Phase 5 plan forked `SchemaV3.TripTask` as a separate frozen class (mirroring the `SchemaV1.TripTask` precedent), but two `@Model` types with the same simple name `"TripTask"` panic SwiftData's cascade traversal on iOS 26.4 in suite mode (the `SchemaTests.cascadeTripToTasks` Trip-deletion test deterministically traps with the fork; passes with a single class). The V2 → V3 lightweight diff for `TripTask` becomes a metadata-identical no-op; SwiftData adds the new `Optional` `ckRecordSystemFields` column on first V3 open via Core Data's automatic column inference. `SchemaV1.TripTask` remains forked because the V1 → V2 lightweight diff has real columns to add.
- `Trip.participantSnapshots` is declared without an `inverse:` keypath (one-way relationship); the companion `TripPersonSnapshot.trip` is also one-way. The design (Decision 7) called for a paired `.cascade` rule, but SwiftData's cascade traversal panics on iOS 26.4 when the trip-deletion path reaches the snapshot ↔ packing-item nullify chain. Snapshot lifetime is enforced by the snapshot-maintenance routine and an explicit trip-deletion sweep instead of the relationship rule.
- `SchemaV2MigrationTests` reduces to a single plan-shape assertion (`AppMigrationPlan` declares V1 + V2). The Phase 3 `freshV2RecordsDefaultCorrectly` and `entityNameIsStableAcrossVersions` tests were removed because they constructed `Schema(versionedSchema: SchemaV2.self)` and `SchemaV1.self` containers in the same test process as the new V3-using tests, triggering SwiftData's same-name-`@Model` resolution conflict. The defaults those tests verified are now covered by the equivalent `SchemaV3MigrationTests` assertions.
- All existing tests that constructed `Schema(versionedSchema: SchemaV2.self)` switch to `Schema(versionedSchema: SchemaV3.self)` (`SchemaTests`, `RulesEngineRunnerTests`, `ApplyTests`, `WhyResolverTests`, `WhyResolverPackingTests`, `DanglingAssigneeTests`, `RenameLocalScopeTests`, `AutoExpandTests`, `PackingFormSaveTests`, `PackingCountsTests`, `PackingPhaseSublineTests`, `PackingSummaryDimmedCountsTests`).
- `docs/agent-notes/persistence.md` updated with three new sections: the V3 schema declaration, why `TripTask` is no longer forked (cascade-panic workaround), the SchemaV3 + Stage A migration test deferral, and the unpaired `Trip.participantSnapshots` rationale.

- Phase 4 — Packing Sheet pre-push review fixes:
  - `PackingListHelpers.phaseSubline` rewritten as a single pass over `trip.packingItems` filtered by a `Set<UUID>` of participant ids, avoiding the O(participants × packingItems) fan-out from the per-participant `counts(for:in:)` loop.
  - Added `PackingListHelpers.countsByPerson(_:)` returning `[UUID: PackingCounts]` in one pass; `PackingSummarySection.body` calls it once and looks up per row instead of calling `counts(for:in:)` inside `ForEach`.
  - `PackingSheet.body` pre-filters `itemsForPerson` once and passes the array to each `PackingItemGroup`, eliminating three full re-scans of `trip.packingItems` per body evaluation.
  - `SheetGroup` gained `scrollAnchor: String` and `matches(_:)` properties; removed the duplicate `scrollAnchor(for:)` / `scrollAnchorRaw(for:)` helpers and the inline group-predicate switch from `PackingSheet`.
  - `PackingItemRow` `.contextMenu` for Edit hoisted from the trailing button to the row level so Edit is available consistently in repack mode (the trailing button is absent there).
  - Removed redundant `skipRestoreLabel` pass-through computed property in `PackingItemRow`.

### Added

- Phase 5 — CKSyncEngine validation harness (`Scramble/ScrambleTests/Sharing/CKSyncEngineValidationHarness.swift`) gating Decision 13. Throwaway Swift Testing suite that creates a private `CKSyncEngine`, saves a custom zone, uploads a probe record, and saves a zone-wide `CKShare(recordZoneID:)`, asserting each step confirms via `sentDatabaseChanges` / `sentRecordZoneChanges` events. Prints the share URL for the manual second-device acceptance step. Gated by `SCRAMBLE_CK_HARNESS=1` so unit-test CI (no iCloud) passes by default; self-deletes its test zone on exit. Pass/fail decides whether Decision 13 stands or we fall back to raw `CKDatabase` for trip-zone entities.

- Phase 5 — CloudKit Sharing spec under `specs/phase-5-cloudkit-sharing/`:
  - `requirements.md` — 13 requirement groups: per-trip share model, `SchemaV3` + persisted person identity, globals-zone strategy, one-time zone migration with idempotent journalled resume and concurrent-device convergence, share invitation + acceptance flows, participant management surface (CKShare participants distinct from the trip people roster), cross-device race handling and engine ownership (owner-only rules engine, trip-global `pinnedByUser` / `userDeletedOnThisTrip`, "Rules last evaluated" subline for participants), CloudKit subscriptions for shared trips, trip ownership identification, failure / offline behaviour, an injectable test seam for CloudKit operations, and CloudKit production schema deployment as a release-prep gate.
  - `design.md` — pivots from per-zone `ModelContainer`s (impossible because SwiftData's `ModelConfiguration.CloudKitDatabase` has no `.shared` case on iOS 26) to one local `tripsLocal` container (`cloudKitDatabase: .none`) plus two `CKSyncEngine` instances behind a `TripSyncEngine` façade. `SchemaV3` adds `TripPersonSnapshot`, `TripZoneState`, and `MigrationJournalEntry` additively (V2 fields kept until V4). Two-stage migration: Stage A (offline-safe SwiftData backfill) and Stage B (CKSyncEngine-driven zone setup with kill-mid-migration resume via the journal). `LocalWriteHook` is the single dirty-marking chokepoint. Snapshot maintenance has three triggers (roster removal, packing-item delete, post-engine sweep). Engine ownership gate plus `isSelfOriginated` echo suppression. Sharing uses zone-wide `CKShare(recordZoneID:)` per trip; `UICloudSharingController` wrapped via `UIViewControllerRepresentable`; share-acceptance routed through `UIApplicationDelegateAdaptor`. UI surfaces: `ShareToolbarButton`, `ParticipantsSection`, participant-side `WhyDisclosure` hide branch, "Rules last evaluated" subline, `MigrationRetryBanner`, Trip List "Syncing…" badge.
  - `decision_log.md` — 13 Enhanced Nygard ADRs: spec naming; per-user master lists and `Person` registry; owner-only rules engine; per-trip atomic + idempotent migration; everyone is read-write; one `ModelContainer` per trip zone (later superseded by Decision 13); `SchemaV3` per-trip person snapshot; silent-push subscription scaffolding lands in Phase 5; `pinnedByUser` / `userDeletedOnThisTrip` are trip-global; two-stage migration with in-globals journal; deterministic zone naming + claim record (claim record dropped under Decision 13); stable subscription IDs (superseded by Decision 13); pivot from per-zone `ModelContainer`s to `CKSyncEngine` after Codex's iOS 26.4 SDK header reading confirmed no public `.shared` case, with post-review amendments documenting `CKShare(recordZoneID:)` usage and `CKSyncEngine.State.add` accepting record IDs (delegate supplies records).
  - `tasks.md` — 36 TDD-paired tasks across 5 phases (Validation gate → Schema + Stage A → Sync infrastructure → Stage B + engine + lifecycle → UI + release-prep). Single stream. The first task is a throwaway end-to-end CKSyncEngine validation harness gating the rest of the phase; if the validation fails, Decision 13 is reopened in favour of raw `CKDatabase` for trip-zone entities.
  - `prerequisites.md` — manual configuration items the implementation cannot perform: iCloud / Push Notifications / Background Modes capabilities, `aps-environment` entitlement, two iCloud accounts for share-flow validation, and Production-environment CloudKit Dashboard schema promotion before TestFlight or App Store release.

- `docs/agent-notes/packing-sheet.md` capturing the Phase 4 module's load-bearing decisions: single-pass count helpers, save-failure UX divergence from `TaskForm`, `.task(id: Set<UUID>)` participant-removal watcher, 500ms VoiceOver focus handoff, `WhyDisclosure.Style` migration, and the engine invariants Phase 4 depends on.
- `specs/phase-4-packing-sheet/implementation.md` — three-level (beginner / intermediate / expert) explanation of the feature with a completeness assessment against the spec.
- `specs/phase-4-packing-sheet/pre-push-review.html` — self-contained pre-push review artifact with commits, per-file diffs, review findings, and decisions.

- Phase 4 — Packing Sheet implementation:
  - `PackingListHelpers` (`Scramble/Timeline/PackingListHelpers.swift`) — `PackingMode`, `PackingCounts`, and pure helpers for `counts(for:in:)`, `summaryStatus`, `progressRatio`, `phaseSubline`, and `sorted` (active-first then case-insensitive name with id tiebreak).
  - `Person.shortDisplayName` — first space-separated token with full-name fallback for sheet add affordance copy.
  - `WhyResolver.reason(for: TripPackingItem, context:)` `@MainActor` overload mirroring the task-side four-branch decision tree, with private `MasterPackingItem` `fetchMaster` overload that logs and returns `nil` on store-fetch failure.
  - `WhyDisclosure.Style` enum (`.tasks(phaseColour:)` / `.packing(personColour:)`) with a `nonisolated ResolvedAppearance` snapshot. `WhyDisclosureView` initializer migrates from `phaseColour:` to `style:`; `TaskRow` updated to call `.tasks(phaseColour:)`. Packing variant uses 6% person-coloured background with no border.
  - `PackingSummarySection`, `PackingSummaryRow`, and private `PackingProgressBar` (`Scramble/Components/PackingSummarySection.swift`) — per-person row with 26pt active avatar, 3pt person-coloured bar (track at 12%, fill at full opacity for ratio < 1.0 and `checkColour` at exactly 1.0), status label, chevron. Soft-impact haptic on tap; combined VoiceOver label with double-tap-to-open suffix; `@AccessibilityFocusState.Binding<UUID?>` wired through for sheet-dismiss focus restoration.
  - `PackingItemRow` (`Scramble/Components/PackingItemRow.swift`) and shared file-scope `SheetGroup` enum — checkbox follows pack vs repack colour rules (pack-active uses person colour 67% / `checkColour` solid; repack-active uses `checkColour` 67% / `checkColour` solid; read-only groups render dashed `textSecondary` placeholder). Spatial gesture separation per Req 6.5: long-press on name region only, `.contextMenu` and `.swipeActions(.trailing)` on trailing action region. WhyResolver caching mirrors `TaskRow`'s on-change pattern. `UIAccessibility.Notification.announcement` posts "Moved to {target group}" after each commit. Single-multiplier `rowOpacity` (1.0 / 0.5).
  - `PackingItemForm` and `PackingItemFormPresentation` (`Scramble/Features/Trips/PackingItemForm.swift`) — single-name form with 200-grapheme cap, no assignee picker (active person implied), no conditions field. Static testable helpers `performAdd / performEdit / isSubmitEnabled / cappedName`. Save-failure semantics differ from `TaskForm`: form retains state and shows inline `"Couldn't save — try again."`; `.edit` failures call `modelContext.rollback()` to restore the prior name.
  - `PackingSheet`, `PackingSheetState`, private `PackingSheetHeader`, and private `PackingItemGroup` (`Scramble/Features/Trips/PackingSheet.swift`) — `.presentationDetents([.large])` + `presentationDragIndicator(.visible)`. Three groups per mode (pack: stillNeedToPack/packed/notBringing; repack: stillInSuitcase/backInSuitcase/leftBehind) with `warn` / `check` / `textSecondary` header colours. Counter reads counts directly off `trip` so sheet body re-renders on `TripPackingItem.state` mutation. `.task(id: Set<UUID>)` keying for participant-removal auto-dismiss avoids spurious refires from SwiftData relationship re-orderings. `headerFocused` set inside a `.task` with 500ms delay so VoiceOver focus lands after the a11y tree builds. Pack-mode `+ Add item for {short-name}` affordance presents `PackingItemForm` via inner `.sheet(item:)`. Escape closes disclosure first, then sheet.
  - `PhaseRow.packingSubline: String?` parameter — composes `{tasks clause} · {packing clause}` when both present; replaces "Nothing here yet" with the packing clause for zero-task packing phases. Threaded into the phase-row accessibility label.
  - `AccordionTimeline.onOpenPackingSheet: (Person, PackingMode) -> Void` callback and `packingSummaryFocus: UUID?` focus binding. `.departureDay` / `.dayBeforeReturn` content gains `PackingSummarySection` rendered as a sibling beneath `TaskListSection`; other phases unchanged.
  - `TripDetailView` wires `.sheet(item: $packingSheetState, onDismiss:)` with focus restoration: still-present originating person → re-focus the summary row via `@AccessibilityFocusState`; absent → set focus to `nil` and post `UIAccessibility.Notification.layoutChanged` so VoiceOver picks the section header heuristically.
  - `UITestSeed` Phase 4 fixtures (`phase4-pack-mode-trip`, `phase4-repack-mode-trip`) — two participants, mixed-state packing items including a dimmed (`currentlyMatchesRules: false`, `pinnedByUser: false`) row to exercise Req 1.6's "dimmed counts toward totals" branch.
  - Tests: `PackingCountsTests`, `PackingStatusTests`, `PackingProgressRatioTests`, `PackingPhaseSublineTests`, `PackingListHelpersSortedTests`, `PackingSummaryDimmedCountsTests`, `PersonShortDisplayNameTests`, `WhyResolverPackingTests`, `WhyDisclosureStyleTests`, `PackingFormSaveTests`. Plus `PackingSheetUITests` with 13 active scenarios and 10 `XCTSkip` placeholders documenting the missing infra hooks (save-failure injection, hardware-keyboard fixture, mid-test fixture mutation, colour-key debug marker, concurrent engine apply hook).

- Phase 4 — Packing Sheet spec under `specs/phase-4-packing-sheet/`:
  - `requirements.md` — 10 requirement groups covering the per-person packing summary block on `departureDay` / `dayBeforeReturn`, the `PackingSheet` bottom sheet with pack and repack modes, manual-item creation, item renaming, `WhyDisclosure` reuse with a person-coloured variant, dimmed rendering for unmatched-non-pinned items, save-failure UX preserving form input, sheet-on-sheet manual-add presentation, participant-removal auto-dismiss, Reduce Motion / VoiceOver / external-keyboard accessibility coverage, and three rules-engine invariants Phase 4 depends on.
  - `design.md` — architecture, components, error handling, and testing strategy. `WhyDisclosureView` migrates to a `Style` enum (`.tasks` / `.packing`); `WhyResolver` gains an `@MainActor` overload for `TripPackingItem`; new `PackingListHelpers`, `PackingSummarySection`, `PackingItemRow`, `PackingSheet`, and `PackingItemForm`. `.task(id: Set<UUID>)` keying for participant-removal watcher avoids spurious re-fires from SwiftData relationship re-orderings. `@AccessibilityFocusState` with a 500ms post-appear delay drives VoiceOver focus handoff; auto-dismiss on participant removal falls back to `UIAccessibility.Notification.layoutChanged`. No schema change — `TripPackingItem` already carries every needed field.
  - `decision_log.md` — 9 Enhanced Nygard ADRs: spec naming; `.large`-only detent (resolves Phase 1 / UI doc open question 1); skip-as-only-removal-mechanism with the Decision 5 interaction; rename-only edit affordance; dimmed items count in progress; no schema bump; no pin/unpin UI in Phase 4; `WhyDisclosure` does not surface master-name divergence after a trip-level rename; manual-item form is sheet-on-sheet.
  - `tasks.md` — 15 TDD-paired tasks across 4 streams with full `blocked_by` dependency graph, ready for parallel execution via `rune next --stream N --claim …`.

### Fixed

- PR #3 review follow-ups (iteration 4):
  - `PhaseDateMapping.durationDays` now returns `Int?` (`nil` for open-ended phases `.weeksBefore` / `.afterTrip`) instead of the sentinel `-1`. Self-documenting and compiler-checked at every call site. `PhaseDateMappingTests` updated; `isCompressed` continues to work because `Optional(0) == 0` and `nil != 0`.

- PR #3 review follow-ups (iteration 3):
  - `PhaseRow.leftColumn` now wraps `PhaseNode` in a `44×44` frame with `TapToToggleModifier`, and the left column itself is `44pt` wide. Req 1.7 / 10.4 require a `44×44` hit target on each phase node, but previously only the right-column header carried the tap modifier — tapping the visible circle (24–28pt) did nothing. `design.md` updated to reflect the split.
  - `design.md` `TaskRow` opacity section now describes the `min(0.5, 0.45) = 0.45` behaviour and explains the chained-`.opacity` history. The earlier `0.22` reference was stale once `TaskRow.rowOpacity` was introduced.
  - `SchemaV2.TripTask.userDeletedOnThisTripRaw` is now `private(set)`. The non-Optional `userDeletedOnThisTrip: Bool` bridge is the single mutation path; external callers can no longer bypass it.
  - `TaskForm.save` carries a comment explaining the intentional dismiss-on-error behaviour (no user-facing error UI per CLAUDE.md "Out of scope for v1"; breadcrumb in `os_log`).
  - `TripDetailView.init` notes that `today` is captured at init time and does not advance across a date boundary while the view stays mounted.

- PR #3 review follow-ups (iteration 2):
  - `WhyDisclosure` namespace + `Reason` enum moved from `WhyResolver.swift` to `WhyDisclosure.swift`. The split made sense during the cross-stream load-order workaround but is misleading now that both streams are merged.
  - `TaskListSection` disclosure-dismiss tap moved from `.onTapGesture` on the VStack to a `Color.clear.onTapGesture` on a `.background { ... }` layer that only mounts when `openDisclosureTaskID != nil`. Prior layout could in principle let the parent gesture compete with `DashedAddButton` and the checkbox; the background-layer pattern keeps button taps and the dismiss target on separate layers and skips the gesture entirely when no disclosure is open.
  - `WhyResolver.fetchMaster` replaces `try?` with `do/catch + modelLogger.error`. The caller still maps `nil` to `.ruleMasterDeleted` (correct user-facing fallback), but a fetch failure now leaves a breadcrumb instead of silently misreporting "master deleted."
  - `PhaseDateMapping` keeps `@MainActor` but documents why: the functions read `Trip.startDate` / `Trip.endDate`, and `Trip` is a `@Model` class whose accessors are MainActor-isolated. A future refactor to plain `Date` parameters could drop the annotation.

- PR #3 review follow-ups:
  - `TaskRow.swift` chained two `.opacity()` modifiers that composed multiplicatively (`0.5 × 0.45 = 0.225`), rendering a completed + rule-no-longer-matches row far dimmer than intended. Replaced with a single `rowOpacity` computed property that takes `min(completedFactor, matchFactor)` — the combined state now matches the dimmer of the two single states (0.45), and strikethrough text continues to distinguish completed.
  - `TaskListSection.toggleComplete`, `TaskListSection.delete`, and `TaskForm.save` previously used `try? modelContext.save()`, silently dropping save errors. Switched to `do/catch` with `modelLogger.error(...)` matching the established pattern in `TripPersistence`, `MasterPersistence`, and `PersonEditor`.
  - `CompressedSpineDot` is now `.accessibilityHidden(true)` — it is purely decorative spine chrome and shouldn't appear as a VoiceOver landing point.

### Changed

- Resolve the swift-format / SwiftLint trailing-comma conflict by making swift-format the single authority. SwiftLint's `trailing_comma` rule is disabled in `.swiftlint.yml` because its `mandatory_comma: true` setting fights swift-format's heuristic when a multi-line array's last element is itself a multi-line constructor (swift-format omits the comma there, SwiftLint demanded it). The idiomatic Swift convention — trailing commas in multi-line literals for cleaner diffs, codified in SE-0439 and Apple's swift-format defaults — is preserved through swift-format alone. swift-format's pending trailing-comma additions to `MidnightAtlas.swift`, `Theme.swift`, `PersonDeleteBlocker.swift` and several test files are folded into this commit so `make format lint` is a no-op going forward.

- CloudKit compatibility for SwiftData relationships: `Trip.participants`, `Trip.tasks`, `Trip.packingItems`, `Person.trips`, `Person.tripPackingItems`, and `Person.masterPackingItems` are now Optional arrays (`[T]? = []`) — non-Optional to-many relationships triggered `NSCocoaErrorDomain Code=134060` (`"CloudKit integration requires that all relationships be optional"`) at `ModelContainer` construction, blocking app launch on a real device. All read sites coalesce via `?? []`. `Person.tripPackingItems` / `Person.masterPackingItems` delete rule downgraded from `.deny` to `.nullify` because CloudKit does not support `.deny`; Person delete-guard enforcement already lived in the UI layer via `PersonDeleteBlocker` per req 9.7, so behaviour is preserved. `SchemaTests`, `DanglingAssigneeTests`, `RulesEngineRunnerTests` and `docs/agent-notes/persistence.md` updated accordingly.

- Pre-push review follow-ups for Phase 3:
  - `Scramble/Scramble/Components/TaskRow.swift` invalidates the cached `WhyResolver.Reason` on `task.currentlyMatchesRules` changes so an open `WhyDisclosure` reflects the rules engine flipping the matching state between `.ruleMatched` ↔ `.ruleNoLongerMatches` without requiring a close-and-reopen.
  - `Scramble/Scramble/Explainability/ConditionsFormatter.swift` routes matched values through `attributeValueDisplay` so the disclosure copy (`"Rain or Snow"`) matches the chips and pickers elsewhere in the UI rather than emitting the raw storage form (`"rain or snow"`). Test expectations in `ConditionsFormatterTests.swift` + `WhyResolverTests.swift` updated to match.
  - `docs/agent-notes/persistence.md` now documents `SchemaV2`, the typealias `TripTask = SchemaV2.TripTask`, the `userDeletedOnThisTripRaw: Bool?` storage rationale (iOS 26.4 Core Data NULL-default constraint), and the versioned-schema policy.
  - `docs/agent-notes/rules-engine.md` documents the `userDeletedOnThisTrip` carve-out (Compute guard + Apply belt-and-braces) and the Phase 3 explainability chain (`WhyResolver`, `ConditionsFormatter`).
  - `specs/phase-3-timeline-tasks/decision_log.md` Decision 12 added: records the `Bool?` storage workaround and the `SchemaV2MigrationTests` on-disk round-trip deferral with rationale and alternatives.

### Added

- Phase 3 Timeline + Tasks implementation: full accordion timeline replacing the static Phase 1 spine, per-phase task lists with manual add/edit/delete, and an inline `WhyDisclosure` for rule-driven explainability.
  - `Scramble/Scramble/Components/PhaseNode.swift` + `CompressedSpineDot.swift` replace `PhaseNodeMarker`: 24/28pt circles with glow-ring on `.current` via dual shadow, packing glyphs (`suitcase.fill` / `shippingbox.fill`) on future/current packing phases, and a 4pt dot at reduced opacity for the compressed `duringTrip` short-trip case (Req 1.x, 3.x).
  - `PhaseRow.swift` (generic over `Content: View`) composes node + header (label + NOW pill + `TaskListHelpers.subline` from `PhaseCounts`) + conditional expanded content; emits its own VoiceOver label via `phaseAccessibilityLabel` and gates tap target on expandability per Req 2.5/2.6 + 10.1.
  - `TaskRow.swift` + `TaskListSection.swift` render filtered, sorted tasks (per `TaskListHelpers.sorted` Req 5.1/5.2 four-bucket rule with `localizedCaseInsensitiveCompare` tiebreaker) plus a `DashedAddButton` affordance; checkbox uses phase colour unchecked / check-green checked per UI doc; long-press body region opens `WhyDisclosure` with a cached `WhyResolver.reason(for:context:)` lookup invalidated by `attributesData`/`name` changes; trailing swipe + `contextMenu` mirror Edit/Delete; manual tasks hard-delete, rule tasks soft-delete via `userDeletedOnThisTrip` flag.
  - `WhyDisclosure.swift` view consuming `WhyDisclosure.Reason` (the namespace + reason enum live in `Explainability/WhyResolver.swift` for cross-stream load order): phase-colour 8% background, 1pt 20% border, 9pt heavy uppercase "WHY?" header in phase colour; renders all four reason cases per UI doc §"Visual treatment — Tasks context".
  - `Features/Trips/AccordionTimeline.swift` owns the `ScrollViewReader` and is the single site that mutates `expandedPhase` and fires the medium-impact haptic; toggle/scroll happen inside the same `withAnimation` block (Req 2.4). `.task` re-runs `TripDetailView.autoExpandPhase` on each appearance with no persistence (Req 2.3 / Decision 4). Disclosure dismissed on phase change.
  - `Features/Trips/TaskForm.swift` + top-level `TaskFormPresentation` enum: name `TextField` (200-char cap via `.onChange`), assignee `Picker` over `trip.participants` with empty-participants placeholder (Req 6.2), Save disabled on empty trimmed name, no `@Query` (Req 6.7). Create sets `source: .manual`, `masterItemID: nil`; edit updates name + assigneePersonID only.
  - `Features/Trips/TripDetailView.swift` rewired: custom `init(trip:today:)` initialises `_expandedPhase` via `State(initialValue: Self.autoExpandPhase(...))` avoiding a one-frame nil flash; `phaseSpine()` replaced by `AccordionTimeline(...)`; new `@State expandedPhase` / `openDisclosureTaskID` / `pendingForm`; `.sheet(item: $pendingForm)` mounts `TaskForm`. `PhaseNodeMarker.swift` deleted.
- Phase 3 helpers (pure value-type, stream 2):
  - `Scramble/Scramble/Timeline/PhaseDateMapping.swift` implements the canonical "Trip-date-to-phase-date mapping" from `requirements.md`: `dateRange` returns `nil` for open-ended phases; `durationDays` returns -1 for `.weeksBefore`/`.afterTrip` and `max(0, (E−S)−1)` for `.duringTrip`; `isCompressed` true iff `.duringTrip && durationDays == 0`.
  - `Scramble/Scramble/Timeline/TaskListHelpers.swift` exposes `PhaseCounts {completed, total, inactive}`, `sorted(_:)` (matching-or-pinned-incomplete → matching-or-pinned-completed → unmatched-non-pinned-incomplete → unmatched-non-pinned-completed; name tiebreak case-insensitive locale-aware), `counts(_:)`, and `subline(_:)` rendering `"{c}/{t} tasks"` with optional `" · +{N} inactive"` (Req 5.1–5.3).
  - `Scramble/Scramble/Explainability/ConditionsFormatter.swift`: walks the `ItemConditions` tree to collect per-attribute master-allowed value sets, intersects with `attributes.selected(...)`, joins within attribute with `" or "` and across attributes with `" + "` in canonical `TripAttribute.allCases` order — boolean shape of `.all`/`.any` does not influence output (UI doc §"Explainability — Behaviour"; Phase 2 Decision 6 follow-through).
  - `Scramble/Scramble/Explainability/WhyResolver.swift`: declares the `WhyDisclosure` namespace + nested `Reason: Equatable, Sendable` (4 cases); `@MainActor` `reason(for:context:)` follows the design table — `.manual` short-circuit, then `masterItemID` lookup via `FetchDescriptor`, then live `conditions.evaluate(against:)` against the trip's current attributes (no snapshot).
  - `TripDetailView.autoExpandPhase(for:today:calendar:)`: pure static helper used by both the `init` and the `.task` modifier; returns the `.current` phase from `state(for:today:start:end:calendar)` unless it's compressed (returns nil) or the phase has neither tasks nor packing affordance (returns nil) — Req 2.3.
- Phase 3 schema migration: `SchemaV2` introduced as a distinct `VersionedSchema`, both `SchemaV1.TripTask` (frozen pre-Phase-3 shape) and `SchemaV2.TripTask` (with `assigneePersonID: UUID?` and `userDeletedOnThisTripRaw: Bool?`) live as namespaced `@Model` types; `typealias TripTask = SchemaV2.TripTask` keeps app code unchanged. `AppMigrationPlan.stages` carries a `.lightweight(fromVersion: SchemaV1.self, toVersion: SchemaV2.self)` migration. The new boolean column is stored as `Bool?` (with a `userDeletedOnThisTrip: Bool` computed bridge treating `nil` as `false`) because SwiftData/CoreData on iOS 26.4 asserts when a nullable column declares a non-nil Swift default — see the comment in `Models/Schema.swift`. `assigneePersonID` is a plain `UUID?` rather than `@Relationship` per Decision 9 so dangling references survive Person removal.
- Phase 3 rules-engine carve-out: `RulesEngine/Snapshots.swift` `TripTaskRef` gains a `userDeletedOnThisTrip: Bool` field populated from the live record at snapshot capture (`RulesEngineRunner.TripSnapshot.capture`). `RulesEngine/Compute.swift` `classifyTaskRefs` short-circuits with `guard !ref.userDeletedOnThisTrip else { continue }` at the top of the loop so deleted rule tasks are neither flagged unmatched nor re-flagged matched. `RulesEngine/Apply.swift` `flagTasks` belt-and-braces guard: skips records whose `userDeletedOnThisTrip == true` before writing `currentlyMatchesRules`, logging `[RulesEngine.skip-flag-userDeleted]` (covers the sync-arrival-vs-snapshot race). `referencedMasterIDs(in:)` is unchanged — deleted refs still count as referenced so the engine doesn't re-create them.
- Phase 3 unit + UI test coverage:
  - `Scramble/ScrambleTests/Persistence/SchemaV2MigrationTests.swift` — round-trip migration on a real on-disk SQLite store (not in-memory), seeds a V1 store with pre-Phase-3 `TripTask` records, opens with `AppMigrationPlan` configured for `SchemaV2`, asserts new fields default to nil/false and that the persistent entity name resolves identically across versions.
  - `Scramble/ScrambleTests/Models/DanglingAssigneeTests.swift` — set `assigneePersonID`, delete the `Person`; assert the `TripTask` survives with the dangling `UUID?` intact (Req 4.8, 9.4).
  - `Scramble/ScrambleTests/Models/RenameLocalScopeTests.swift` — renaming a rule-driven `TripTask` does not propagate to the source `MasterTaskItem` (Req 7.6).
  - `Scramble/ScrambleTests/RulesEngine/PerTripScopeTests.swift` — deleting a rule task on trip A does not affect trip B (Req 7.5 / Decision 7).
  - `ScrambleTests/RulesEngine/{Compute,Apply}Tests.swift` extended with `userDeletedOnThisTrip` coverage across all four `(currentlyMatchesRules, masterMatches)` quadrants and the Apply belt-and-braces skip.
  - `Scramble/ScrambleTests/Timeline/PhaseDateMappingTests.swift`, `TaskListOrderingTests.swift`, `PhaseCountsTests.swift` — canonical mapping table coverage, four-bucket sort + non-ASCII name comparison, subline-format edge cases.
  - `Scramble/ScrambleTests/Explainability/ConditionsFormatterTests.swift`, `WhyResolverTests.swift` — AND-across / OR-within join behaviour, `TripAttribute.allCases` ordering determinism, empty-intersection empty string; all four `Reason` branches plus the regression test that calls the resolver twice with mutated trip attributes between calls.
  - `Scramble/ScrambleTests/Trips/AutoExpandTests.swift` — `autoExpandPhase` returns `.current` on a normal trip, `nil` when the current phase is non-expandable, `nil` for the compressed-`duringTrip` short-trip case (covered via "compressed never wins iteration" since `duringTrip` cannot simultaneously be both compressed and current under the mapping).
  - `Scramble/ScrambleUITests/TimelineAndTaskUITests.swift` — `testAccordionAutoExpandsCurrentPhase`, `testOnlyOnePhaseExpandedAtATime`, `testCompressedDuringTripIsNotTappable`, `testCheckboxToggleAndStrikethrough`, `testLongPressOpensWhyDisclosure`, `testOnlyOneDisclosureOpenAtATime`, `testTapElsewhereDismissesDisclosure`, `testSwipeRevealsEditAndDelete`, `testManualTaskAddPersistsAcrossLaunch`, `testRuleDeletionPersistsAcrossReevaluation`, `testAssigneePickerEmptyParticipants`. `testSublineWrapsAtAX2` is `XCTSkip`-ed (Dynamic Type launch arg not wired in the seed; deferred to manual verification per Req 5.4). New accessibility IDs: `tripDetail.phaseNode.{phase}`, `tripDetail.phaseHeader.{phase}`, `tripDetail.accordion.expanded`, `tripDetail.addTaskButton.{phase}`, `tripDetail.whyDisclosure.{taskName}`, `tripDetail.taskRow.{taskName}`, `tripDetail.taskListSection.{phase}`.
- `Scramble/Scramble/Persistence/UITestSeed.swift` extended with three Phase 3 fixtures: `phase3-trip-with-tasks` (trip in `duringTrip` with rule-driven + manual tasks across multiple phases), `phase3-one-day-trip` (single-day trip exercising compression), `phase3-trip-no-participants` (trip with zero participants for the empty-picker test).

### Changed

- `Scramble/Scramble/Models/Enums.swift`: `PhaseNodeState` enum moved here from the deleted `PhaseNodeMarker.swift` so the type survives the component file's removal.
- Phase 3 spec progress: `specs/phase-3-timeline-tasks/tasks.md` updated to mark all 22 tasks complete across the 4 work streams.

### Removed

- `Scramble/Scramble/Components/PhaseNodeMarker.swift` — superseded by `PhaseNode.swift` + `CompressedSpineDot.swift` once `TripDetailView.phaseSpine()` was replaced with `AccordionTimeline`.
- `Scramble/Scramble/Models/TripTask.swift` — the top-level `@Model` is now defined inside `SchemaV2` in `Models/Schema.swift`; the `typealias TripTask = SchemaV2.TripTask` keeps every existing call site compiling unchanged.

- Phase 3 spec (`specs/phase-3-timeline-tasks/`): `requirements.md` (10 EARS requirement groups + canonical trip-date-to-phase-date mapping, resolving open question 2), `design.md` (architecture for `AccordionTimeline`, `PhaseRow`, `TaskRow`, `WhyDisclosure`, `TaskForm`; pure helpers `PhaseDateMapping` / `ConditionsFormatter` / `WhyResolver`; rules-engine carve-out in `classifyTaskRefs` + `flagTasks`; `SchemaV2` with namespaced `@Model TripTask` and lightweight migration; testing strategy covering all ACs), `decision_log.md` (11 decisions including soft-delete tombstone on `TripTask.userDeletedOnThisTrip`, `UUID?` over `@Relationship` for assignee, versioned-schema policy adoption, deferred CloudKit merge to Phase 5), and `tasks.md` (22 tasks across 4 work streams with TDD pairing and `blocked_by` dependencies).
- Phase 2 UI + integration tests (`Scramble/ScrambleUITests/`): `MasterListsCRUDUITests.swift` covers master task create/edit and destructive-confirmation surfacing, plus the seeded-packing-item rendering / edit / destructive-confirmation flows; `ConditionsEditorUITests.swift` covers chip-selection round-trip (AC 3.6), empty save persisting as `.always` (AC 3.4), and the `AdvancedConditionView` placeholder + "Reset to simple" confirmation surfacing (AC 3.7a–c); `MasterPackingEmptyStateUITests.swift` covers the AC 2.7 zero-Person empty state copy and hidden "+ Add item" affordance; `RulesEnginePopulationUITests.swift` covers the AC 5.1 trip-create and AC 5.2 trip-edit rule population paths, reading rule-driven `TripPackingItem` flags via the new debug accessibility markers; `ColdLaunchSequencingUITests.swift` proves the `ScrambleApp.init()` cold-launch scan (AC 5.4) completes before `TripsTab`'s auto-open task fires; `RootViewScenePhaseTests.swift` covers the AC 5.7 carve-out (cold-launch sequence MUST NOT invoke the runner) and the `.background → .active` re-entry firing exactly once via a debug counter exposed in `RootView`; `PersonDeletionGuardUITests.swift` proves Phase 1's `PersonDeleteBlocker` surfaces a master-list reference in the AC 8.2 alert when a Person owns a `MasterPackingItem`.
- New debug-only seed fixtures (`Scramble/Scramble/Persistence/UITestSeed.swift`): `master-lists-empty-with-person` (one Person + one seeded MasterPackingItem so packing edit/delete UI tests avoid driving the `Picker(.menu)` person selector); `master-task-advanced-conditions` (out-of-domain weather value to force AdvancedConditionView); `phase2-rules-fixture` and `phase2-rules-fixture-sun-trip` (Person + rain-conditioned MasterPackingItem with/without a Weather=Sun trip); `phase2-rules-cold-launch` (qualifying trip + always-matching MasterTaskItem); `person-with-master-packing-only` (Person owning a MasterPackingItem on an editable trip).
- Debug-only accessibility hooks for the UI test suite: `Scramble/Scramble/Features/Trips/TripDetailView.swift` exposes hidden `tripDetail.{packingItem|task}.{matching|unmatched}.{name}` markers so tests can read `currentlyMatchesRules` without a Phase 3 timeline UI; `Scramble/Scramble/Features/Root/RootView.swift` exposes a `rulesEngine.scenePhaseRunnerCalls.<n>` counter; `Scramble/Scramble/Features/MasterLists/MasterPackingEditor.swift` adds `masterPacking.personPicker` to the Person picker; `Scramble/Scramble/Features/MasterLists/ConditionsEditor.swift` adds `accessibilityAddTraits(.isSelected)` to selected chips. All scoped under `#if DEBUG` so production builds ship without them.

### Fixed

- Phase 2 pre-push review hardening: `Scramble/Scramble/RulesEngine/Compute.swift` now uses `Dictionary(uniquingKeysWith:)` so a CloudKit-merge race that briefly surfaces duplicate master UUIDs does not trap. `Scramble/Scramble/ScrambleApp.swift` and `Scramble/Scramble/Features/Root/RootView.swift` replace silent `try?` on the cold-launch and scenePhase runner calls with `do/catch` that logs `[RulesEngine.cold-launch-failed]` / `[RulesEngine.scenePhase-failed]`. `Scramble/Scramble/RulesEngine/RulesEngineRunner.swift` hoists `fetchMasterTaskSnapshots` / `fetchMasterPackingSnapshots` out of the per-trip loop in `runForAllNonPastTrips`, dispatching to a private `runForTrip(_:masterTasks:masterPacking:)` overload so 20 trips × 200 masters no longer multiplies into 8,000 model materialisations per AC 5.5 budget run. `Scramble/Scramble/RulesEngine/Apply.swift` `insertAddedPacking` batch-fetches `Person` via a single `idSet.contains` predicate and resolves from a `[UUID: Person]` map, eliminating the per-item `FetchDescriptor<Person>`. `Scramble/Scramble/Features/MasterLists/MasterPersistence.swift` `resolvePerson` logs `[MasterPersistence.resolvePerson]` on fetch failure instead of silently dropping the person.
- Phase label helper consolidated: `Scramble/Scramble/Models/Enums.swift` adds `Phase.displayName` as the single source of truth for the seven canonical phase labels; `Scramble/Scramble/Features/Trips/TripDetailView.swift` `label(for:)` and `Scramble/Scramble/Features/MasterLists/MasterListLabels.swift` (deleted) both routed through the new property. `MasterTasksList.swift` and `MasterTaskEditor.swift` now call `phase.displayName`.
- `+ Add` affordance consolidated: new `Scramble/Scramble/Components/DashedAddButton.swift` replaces three duplicate dashed-border button helpers (`TripListView.newTripButton`, `MasterTasksList.addButton`, `MasterPackingList.addButton`).

- `RootView` scenePhase trigger (AC 5.7): the previous `previousScenePhase == .background, newPhase == .active` guard never fired because iOS delivers `.background → .inactive → .active` with `.inactive` in between, masking the direct `.background → .active` edge that `onChange` observes. Replaced with a `hasBeenBackgrounded` flag that latches on `.background` and resets after firing the runner on the next `.active`. The cold-launch carve-out remains (the initial `nil → .inactive → .active` sequence never observes `.background`).

- Phase 2 rules-engine lifecycle trigger wiring: `Scramble/Scramble/ScrambleApp.swift` runs `RulesEngineRunner.runForAllNonPastTrips()` in `init()` after the `UITestSeed` block so the cold-launch scan (AC 5.4) completes before `WindowGroup` mounts any view. `Scramble/Scramble/Features/Root/RootView.swift` adds an `@Environment(\.scenePhase)` + `previousScenePhase` `@State` pair with an `onChange` handler that fires the runner only on a true `.background → .active` transition (AC 5.7) — the `nil → .inactive → .active` cold-launch sequence is excluded because `previousScenePhase` is never `.background` at that point. `Scramble/Scramble/Features/Trips/TripListView.swift` and `TripDetailView.swift` invoke `RulesEngineRunner.runForTrip` in the trip-editor save closures after `modelContext.save()` succeeds, satisfying AC 5.1 (create) and AC 5.2 (edit).

- Phase 2 Master Lists UI: `Scramble/Scramble/Features/MasterLists/MasterTaskDraft.swift` and `MasterPackingDraft.swift` — value-type drafts with per-field `validate()` (empty/whitespace name → `.name`; nil person → `.person`) and `init(from:)` for edit mode. `MasterPersistence.swift` — `@MainActor` helper with `createTask` / `applyTask` / `deleteTask` and `createPacking` / `applyPacking` / `deletePacking`; mirrors `TripPersistence` by mutating but never calling `context.save()` so the editor closure owns the mutate → save → run-engine sequence. `ConditionsEditor.swift` — per-attribute chip multi-select in a `LazyVGrid(.adaptive(minimum: 88))`, selected chips fill with theme accent, unselected wear a 1pt `surfaceBorder` outline. `AdvancedConditionView.swift` — AC 3.7 read-only placeholder rendering `prettyPrinted()` text + "Reset to simple" affordance with the AC 3.7c confirmation copy ("On the next re-evaluation this item will match every non-past trip until you save new conditions."). `MasterTaskEditor.swift` and `MasterPackingEditor.swift` — Form-based create/edit sheets with name + Phase/Person picker + conditions (chip editor or advanced placeholder per `AttributeSelections.from(_:)`), inline validation per AC 1.5 / 2.5, delete confirmation, and the three-step save closure (persistence → `modelContext.save` → `RulesEngineRunner.runForAllNonPastTrips`) per the design Error Handling table. `MasterTasksList.swift` — `@Query(sort: \.name)` grouped in-memory by `Phase` in canonical order with empty groups omitted (AC 1.1), dashed-border "+ Add task" affordance, `.sheet(item:)` routing to the editor in `.create` or `.edit` mode. `MasterPackingList.swift` — `@Query` all items + people sorted by `Person.name` ascending with per-person counts and empty groups omitted (AC 2.1); AC 2.7 empty state ("No people yet") replaces the list and hides the "+ Add item" affordance when `Person` count is zero. `Phase.displayName` (`Scramble/Scramble/Models/Enums.swift`) is the shared label for both the master-list editor and the trip-detail spine — `TripDetailView.label(for:)` is removed. `MasterListsTab.swift` rewritten to host `MasterPackingList()` / `MasterTasksList()` behind the existing segmented control (placeholder copy retired).
- `Scramble/ScrambleTests/MasterLists/MasterDraftTests.swift`: Swift Testing coverage of `MasterTaskDraft.validate()` (valid, empty, whitespace-only) and `MasterPackingDraft.validate()` (valid, empty name, nil person, both invalid).

- Phase 2 rules-engine compute/apply/runner: `Scramble/Scramble/RulesEngine/Compute.swift` — pure value-type `compute(trip:masterTasks:masterPacking:) -> Plan` implementing the 4-way matrix (currentlyMatchesRules × condition-evaluation) with the asymmetric exclusion table (pin / completed / packed / repacked / excluded protect against `toFlagUnmatched` but never `toFlagMatched` per AC 6.1–6.3), `.manual` source pre-filter (AC 4.6), dangling-master classification as condition-false (AC 4.7), and dedup against existing trip references including duplicate sync-race refs (AC 6.4). `Scramble/Scramble/RulesEngine/Apply.swift` — `@MainActor apply(plan:context:)` that short-circuits on `Plan.isEmpty`, fetches the trip by id (logs `[RulesEngine.skip-trip]` and returns on miss), inserts rule-driven `TripTask`/`TripPackingItem` with snapshot fields, resolves `Person` per id (logs `[RulesEngine.skip-packing-orphan]` on miss), batches flag updates via a single `FetchDescriptor` per kind with `idSet.contains($0.id)`, and propagates save errors via `[RulesEngine.save-failed]`. `Scramble/Scramble/RulesEngine/RulesEngineRunner.swift` — `@MainActor` struct exposing `runForTrip(_:)` and `runForAllNonPastTrips(today:calendar:)` with calendar-day cutoff and per-trip do/catch so a single failure doesn't abort the fan-out; snapshot capture skips `MasterPackingItem.person == nil` via `[RulesEngine.skip-orphan-master]`.
- `Scramble/ScrambleTests/RulesEngine/ComputeTests.swift`: table-driven coverage of the 4-way matrix, full exclusion table (pin / completed / packed / repacked / excluded — symmetric matched-side cases), `.manual` invisibility, dangling-master classification, `nil` `masterItemID` ignored, `.match(_, anyOf: [])` evaluates false, deterministic sort invariants for all four Plan collections, and the 200-master worst-case fan-out.
- `Scramble/ScrambleTests/RulesEngine/ComputeIdempotenceTests.swift`: SplitMix64-seeded property-based tests over 40 random snapshots each for determinism (`compute(x, m1, m2) == compute(x, m1, m2)`), idempotence (apply simulated as a value-type rewrite, second compute returns empty Plan — AC 5.6), and no-spurious-adds (every-master-false → empty `toAdd*`).
- `Scramble/ScrambleTests/RulesEngine/ApplyTests.swift`: in-memory `ModelContainer` integration coverage of empty-plan short-circuit (no `hasChanges` after apply), insertion fields snapshotted per AC 4.3/4.4, missing-person skip, flag-only mutation preserving name/phase/state/person on `toFlag*`, missing-trip / missing-trip-item skip-and-continue, and a mixed plan exercising all four Plan collections in one save.
- `Scramble/ScrambleTests/RulesEngine/RulesEngineRunnerTests.swift`: end-to-end runner coverage — `runForTrip` populates rule-driven trip-level items, second call is idempotent (AC 5.6), `runForAllNonPastTrips` returns `[]` on an empty store and skips past trips by `endDate` calendar-day, snapshot capture skips orphan-person master items, AC 7.1/7.2/7.3 pin master rename / phase change / person change as non-propagating to existing trip-level items, and the per-trip catch loop processes both trips when neither throws.
- Phase 2 conditions-editor bridge: `Scramble/Scramble/Features/MasterLists/AttributeSelections.swift` — value-type per-attribute chip selections (`byAttribute: [TripAttribute: Set<String>]`) with `toConditions()` emitting the v1 `.all([.match(...)])` shape in `TripAttribute.allCases` declaration order with values sorted alphabetically (AC 3.5), `from(_:)` decoding back to selections or returning nil for non-v1 shapes (nested groups, top-level `.any`, non-`.match` children, empty `anyOf`, out-of-domain values — AC 3.7), and `isInDomain()` defensive guard (AC 3.8). `Scramble/ScrambleTests/MasterLists/AttributeSelectionsTests.swift` covers round-trip on the simple shape, the four rejection cases, and a parameterised PBT round-trip over generated v1-shape selections.
- Phase 2 advanced-condition pretty printer: `Scramble/Scramble/Features/MasterLists/ItemConditions+PrettyPrint.swift` — recursive `prettyPrinted(indent:)` rendering `.always` → "always", `.match(attr, vals)` → "attr is V1 or V2" via `attributeValueDisplay`, and `.all`/`.any` as multi-line "all of:"/"any of:" with two-space indented children. Lives under `Features/MasterLists/` (not `Models/Codable/`) so the persisted model file stays CloudKit-pure. `Scramble/ScrambleTests/MasterLists/ItemConditionsPrettyPrintTests.swift` covers single/multi values, nested `.all`/`.any`, the indent parameter, and a ZWJ-emoji-in-value defence.
- Phase 2 rules-engine foundation value types: `Scramble/Scramble/RulesEngine/Snapshots.swift` (`TripSnapshot`, `TripTaskRef`, `TripPackingItemRef`, `MasterTaskSnapshot`, `MasterPackingSnapshot`) and `Scramble/Scramble/RulesEngine/Plan.swift` (`Plan`, `TripItemRef`). All types are `Sendable` value types; master snapshots are also `Hashable`. `Plan.init` sorts `toAddTasks`/`toAddPacking` by master id ascending and `toFlag{Unmatched,Matched}` by `(kind, id)` ascending per the deterministic-ordering invariant (AC 4.9). These types are the contract `compute(...)` / `apply(plan:context:)` will consume in subsequent Phase 2 tasks.
- `Scramble/ScrambleTests/RulesEngine/PlanTests.swift`: Swift Testing suite covering `Equatable`/`Hashable`/`Sendable` conformance on all five snapshot types, `TripItemRef.Kind` raw values (`"task"`/`"packing"`), `Plan` sort invariants, and `Plan.isEmpty`.

### Changed

- `String.attributeValueDisplay` (`Scramble/Scramble/Features/Trips/TripAttributeOptions.swift`) marked `nonisolated`. The property is pure string manipulation but defaulted to `@MainActor` under the project's MainActor-default isolation, which made it unreachable from `nonisolated` consumers (the new `ItemConditions.prettyPrinted` extension hits it via a key-path map). MainActor callers in `TripEditorView`/`TripDetailView` are unaffected.
- `Scramble/ScrambleTests/RulesEngine/PlanTests.swift`: removed trailing commas in collection literals so `make lint` (`swiftlint --strict`) passes cleanly. No test logic change.
- `ItemConditions` (`Scramble/Scramble/Models/Codable/ItemConditions.swift`) now conforms to `Hashable` in addition to `Codable`/`Equatable`/`Sendable`. Required so `MasterTaskSnapshot` and `MasterPackingSnapshot` can synthesise `Hashable` (master snapshots embed `ItemConditions`).

- `docs/implementation-phases.md`: post-Phase-1 plan splitting remaining work into Phases 2–6 (Master Lists + Rules Engine, Timeline + Tasks, Packing Sheet, CloudKit Sharing, Notifications + Polish). Documents the ordering rationale (rules engine before its UI consumers) and maps the three Phase 1 open questions to the phases that resolve them.
- `specs/phase-2-rules-engine/`: full spec for Phase 2 — `requirements.md` (8 requirement groups, 39 acceptance criteria), `design.md` (snapshot/compute/apply pipeline with value-type contracts, Master Lists editor architecture, exclusion-asymmetry table, five re-evaluation triggers including the `scenePhase` foreground catch-up), `decision_log.md` (7 decisions covering simple-table conditions editor, snapshot-not-live-link semantics, no-seed posture, trigger set, three-step capture/compute/apply split, deferred matching-clauses function for Phase 3, and the peer-review delta that added the foreground trigger), `tasks.md` (29 TDD-paired tasks across 6 phases, all stream 1, with `blocked_by` dependencies).
- Phase 1 pre-push review hardening: `CodableBridge` (`Scramble/Scramble/Models/Codable/CodableBridge.swift`) consolidates the JSON encode/decode + `modelLogger.error` logging that was open-coded across `Trip.attributes`, `MasterTaskItem.conditions`, and `MasterPackingItem.conditions`. Decode failures fall back to a caller-supplied default; encode failures return empty `Data` so the next successful save overwrites the corrupt blob.
- `UITestConstants` (`Scramble/Scramble/Persistence/UITestConstants.swift`) centralises the UI-test launch contract: `UITestArguments.uitestFlag` / `uitestValue` / `seedFixtureKey` (always), and `DebugAccessibilityID.modelStoreInMemory` / `modelStoreCloud` (DEBUG-only). Eliminates the literal-string sprawl across `EnvironmentProbe`, `UITestSeed`, and `RootView`.
- `specs/phase-1-foundation/implementation.md` generated by the `explain-like` skill: beginner / intermediate / expert walkthroughs of the Phase 1 implementation plus a Completeness Assessment mapping every AC 1.1–10.3 to its current implementation status.

### Changed

- AC 10.3 spec adherence: `PersonAvatar` now renders on Trip List rows (`.compact`) and the Trip Detail header (`.standard`) in addition to the trip editor (`.standard`). Previously only the editor surface used the avatar primitive — the spec required all three.
- AC 6.2 chip display: `TripDetailView.chipRow` now renders attribute values via `String.attributeValueDisplay` (e.g. "Rain") instead of the raw lowercase storage value ("rain"), matching the form the trip editor's picker rows already used. `TripCRUDUITests.testEditTripAttributesUpdatesChips` updated to assert `app.buttons["Rain"]`.
- AC 6.6 delete error handling: `TripDetailView`'s delete-confirmation button now wraps `try modelContext.save()` in `do/catch`; on success it dismisses, on failure it `modelContext.rollback()` and surfaces a "Delete failed — try again." transient toast. Previously the save error was swallowed via `try?`.
- `TripEditorView.orderedSelectedPeople` and `TripPersistence.resolveParticipants` switched from `O(N·M)` linear scans (`compactMap { ids.first(where:) }`) to `Dictionary(uniqueKeysWithValues:)` lookups. Touches no behaviour, only complexity.
- `TripEditorView.singleSelectPicker` moved the `selected` read inside the `Binding.get` closure so the picker observes live `draft.attributes` updates rather than a body-build-time capture. Previously worked by virtue of `@State` body rebuilds but was fragile.
- `Person.initial` and `PersonAvatar`'s initial both delegate to a single `String.firstGraphemeUppercased` extension. Previously each carried its own copy of the "first grapheme uppercased, '?' on empty" logic.
- `TripListView` + `TripDetailView` consolidated duplicate helpers: `formatDateRange` → `formatTripDateRange` free function next to `TripPersistence`; `orphanMessage(count:)` → `TripPersistence.orphanedParticipantMessage(count:)`.
- `TripListView.activeTrips` dropped a redundant sort (`@Query(sort: \Trip.startDate)` already returns ascending order); `previousTrips` keeps the explicit `endDate` descending sort mandated by AC 5.2.
- Lint pass: trailing commas removed across `Models/Schema.swift`, `Theme/MidnightAtlas.swift`, `ScrambleTests/Models/ItemConditionsTests.swift`, `ScrambleTests/Persistence/SchemaTests.swift`; `MidnightAtlas.rgba` opening-brace placement fixed; long lines in `MasterListsTab.placeholderText` reflowed to multi-line strings; `ScrambleUITestsLaunchTests` template overrides `runsForEachTargetApplicationUIConfiguration` as `static` (was `class`) per swiftlint's `static_over_final_class` rule; `TripDetailView.state` annotated `// swiftlint:disable:next cyclomatic_complexity` (genuine 7-phase × 3-state branching with no simplification gain). `make lint` now exits cleanly.
- `CLAUDE.md`'s "Project status" paragraph updated to reflect Phase 1 having landed (was stale: "no real models, views, or rules engine have been written yet").

### Added

- Phase 1 UI test suite: `AppLaunchUITests` (`Scramble/ScrambleUITests/AppLaunchUITests.swift`) verifies `XCUIApplication.launchArguments = ["-uitest", "1"]` set BEFORE `launch()` produces the in-memory `ModelContainer`, asserted via the debug-only `modelStore.in-memory` accessibility identifier on `RootView`.
- `RootNavigationUITests` (`Scramble/ScrambleUITests/RootNavigationUITests.swift`) covering cold-launch outcomes (zero / one-qualifying / one-non-qualifying / two-qualifying trips), tab bar hidden on Trip Detail and restored on pop, and `testAutoOpenDoesNotRefireOnTabSwitch` (auto-open guard survives a Trips → Master Lists → Trips round-trip per AC 5.7).
- `TripCRUDUITests` (`Scramble/ScrambleUITests/TripCRUDUITests.swift`) exercising end-to-end create (tap +New, fill name, save → row appears), edit (open detail → menu → Edit → toggle Weather "Rain" → save → chip "rain" appears), and delete (open detail → menu → Delete Trip → confirm → row disappears).
- `UITestSeed` (`Scramble/Scramble/Persistence/UITestSeed.swift`, DEBUG-only) parses the launch argument `-seed-fixture <name>` and pre-seeds the in-memory container before SwiftUI's first body pass. Fixtures: `one-qualifying-trip`, `one-non-qualifying-trip`, `two-qualifying-trips`, `single-editable-trip`. Production binaries cannot be seeded.

### Changed

- `RootView` (`Scramble/Scramble/Features/Root/RootView.swift`) attaches a 1pt invisible accessibility element under `#if DEBUG` exposing `modelStore.in-memory` (or `modelStore.cloud` in production-detection contexts) so UI tests can assert the host app booted with the in-memory store. The marker is compiled out of release builds.
- `ScrambleApp.init` (`Scramble/Scramble/ScrambleApp.swift`) calls `UITestSeed.applyIfRequested(to: ModelStore.shared)` under `#if DEBUG`, ensuring fixtures land in the main context before `RootView`'s `@Query` evaluates and the auto-open `.task` fires.
- `TripDetailView.swift` adds `.accessibilityLabel("Trip actions")` to the toolbar `Menu`'s `ellipsis.circle` icon so the button is reachable by both VoiceOver and `XCUIElementQuery`.

- Phase 1 cold-launch auto-open: `singleQualifyingTrip(in:today:calendar:)` helper (`Scramble/Scramble/Features/Trips/TripsTab.swift`) that returns the single qualifying trip when exactly one matches (start ≤ today + 2 calendar days AND end ≥ today at calendar-day granularity); zero or two-or-more qualifying trips return `nil` per AC 5.6.
- Swift Testing suite `TripsTabPredicateTests` (`ScrambleTests/Features/TripsTabPredicateTests.swift`) table-driven over empty trips, single qualifying, non-qualifying (start past today+2 / end before today), two qualifying, boundary edges (start = today+2, end = today, start = today), time-of-day on `today` not affecting the calendar-day comparison, and mixed sets where exactly one qualifies.

### Changed

- `TripsTab` (`Scramble/Scramble/Features/Trips/TripsTab.swift`) replaced the `ContentUnavailableView` placeholder with the real navigation stack: `NavigationStack(path:)` driving a `TripListView` root and a `navigationDestination(for: Trip.self)` that renders `TripDetailView` (with `.toolbar(.hidden, for: .tabBar)` applied at the destination). On mount, `.task(id: "trips-tab-mount")` runs once — guarded by `@State didAttemptAutoOpen` set BEFORE the path mutation — and pushes the single qualifying trip onto the path. Uses `@State` (NOT `@SceneStorage`) by design so state restoration cannot suppress auto-open across kill-then-relaunch; a code-review-visible comment documents this.

- Phase 1 trip editor + people management: `TripDraft` value-type (`Scramble/Scramble/Features/Trips/TripDraft.swift`) with `validate(calendar:)` returning per-field error map at calendar-day granularity per Decision 3 — empty/whitespace name maps to `.name`, end-day before start-day maps to `.dateRange`, both invalid maps to both; `newDraft()` for create mode and `init(from: Trip)` for edit mode (MainActor-isolated since it reads `@Model` properties).
- `TripEditorView` (`Scramble/Scramble/Features/Trips/TripEditorView.swift`) Form with name + date pickers, attribute pickers (single-select Picker for Duration/Transport/Scope/Purpose, multi-select toggle rows for Weather), people section listing selected participants and an "Add existing person" disclosure with `PersonAvatar` rows, inline "Create new person" affordance, save closure pattern returning `Bool` so parents own the persist step, `ScrollViewReader`-based scroll-to-attribute on appear when launched from a chip tap.
- `TripAttributeOptions` (`Features/Trips/TripAttributeOptions.swift`) with canonical v1 values (Duration: short/long; Transport: car/plane/train; Scope: domestic/international; Weather: sun/rain/cold/hot; Purpose: work/leisure) and `TripAttribute.displayName` + `isMultiSelect` extensions.
- `PersonEditor` (`Features/People/PersonEditor.swift`) sheet with name field, 4-column palette swatch grid for the eight theme colours, default-colour assignment via `PersonPalette.nextUnusedKey(among:)`, non-blocking duplicate-color advisory naming the conflicting person, save inserts a new `Person` and surfaces it via an out-binding.
- `TripPersistence` (`Features/Trips/TripPersistence.swift`) `@MainActor` helper centralising orphaned-participant tolerance: `resolveParticipants(ids:in:)` uses `FetchDescriptor<Person>(predicate: #Predicate { idSet.contains($0.id) })` to drop unresolved IDs and return them to the caller; `create(from:in:)` and `apply(_:to:in:)` apply the draft and report orphans for toast surfacing.
- `TransientToast` view modifier (`Components/TransientToast.swift`) presenting a self-dismissing bottom banner over the host view; used by `TripListView` and `TripDetailView` to surface orphaned-participant counts after save.
- Person delete with UI-guard enforcement per Decision 16 / AC 9.7: context menu on every person row in the editor offers "Delete person from app…"; on tap, checks `Person.tripPackingItems` and `Person.masterPackingItems` inverse traversals — empty references trigger a confirmation dialog and `context.delete`, non-empty triggers an alert listing the referencing trips and master items.
- Swift Testing suite `TripDraftTests` (`ScrambleTests/Features/TripDraftTests.swift`) table-driven over: empty name, whitespace-only name, end before start, both invalid, all valid, same-day start/end, and same-calendar-day with different times-of-day.

### Changed

- `TripListView` (`Scramble/Scramble/Features/Trips/TripListView.swift`): replaced the "+ New Trip" placeholder sheet with a real `TripEditorView` in `.create` mode wired through `TripPersistence.create` + `context.save`, surfacing orphaned-participant counts via the new transient toast.
- `TripDetailView` (`Scramble/Scramble/Features/Trips/TripDetailView.swift`): replaced the Edit placeholder sheet with `TripEditorView` in `.edit(trip)` mode passing the `editAttributeFocus` for chip-tap-to-scroll; save path goes through `TripPersistence.apply` + `context.save` with transient-toast surfacing of orphans.

- Phase 1 trip surfaces: `TripListView` (`Scramble/Scramble/Features/Trips/TripListView.swift`) with `@Query`-driven Active and Previous sections split in memory by calendar-day end-date comparison; Active sorted by start date ascending, Previous sorted by end date descending and collapsed by default via `Section(isExpanded:)`; each row renders trip name + abbreviated date range + `LocalizedTripStatus`; `NavigationLink(value: trip)` pushes onto the surrounding navigation path; "+ New Trip" dashed-border button beneath the Active section (placeholder sheet wired in a later task).
- `TripDetailView` (`Scramble/Scramble/Features/Trips/TripDetailView.swift`) scaffold with a non-scrolling header (name + date range + `LocalizedTripStatus`), horizontally scrolling attribute chip row that flattens `TripAttributes` selections into tap-to-edit chips, vertical 2pt spine with seven labelled `PhaseNodeMarker` rows whose past/current/future state is computed from today's calendar day against the trip's `startDate`/`endDate` (with `dayBefore`/`dayBeforeReturn`/`afterTrip` derived from those bounds), Edit/Delete affordances via a toolbar `Menu`, `.confirmationDialog` for delete that calls `modelContext.delete(trip)` + `dismiss`, and `.toolbar(.hidden, for: .tabBar)` to hide the root tab bar inside Trip Detail.
- Phase 1 app shell: `RootView` (`Scramble/Scramble/Features/Root/RootView.swift`) with a two-tab `TabView` (Trips / Master Lists) using `suitcase` and `list.bullet.rectangle` SF Symbols. Default selected tab is Trips. Liquid Glass treatment is system-applied to the root TabView on iOS 26.
- `TripsTab` (`Scramble/Scramble/Features/Trips/TripsTab.swift`) placeholder view wrapping a `NavigationStack` with `ContentUnavailableView` describing that real content arrives in later tasks.

### Changed

- `MasterListsTab` (`Scramble/Scramble/Features/MasterLists/MasterListsTab.swift`): replaced the `ContentUnavailableView` placeholder with a segmented control (Packing Items / Tasks) backed by a nested `Segment` enum; per-segment `ContentUnavailableView` explains that master-list editing arrives in a later phase. No item creation, editing, or deletion affordances per AC 7.3.

### Changed

- `ScrambleApp` now references `RootView` directly, wires `.environment(\.theme, .midnightAtlas)`, and uses `ModelStore.shared` as the model container — replacing the Xcode template's inline schema, in-memory-on-test branching, and fallback container construction.

### Removed

- Template scaffolding files `Scramble/Scramble/Item.swift` and `Scramble/Scramble/ContentView.swift`. The model container schema now comes from `SchemaV1` and the root view from `RootView`.

- Phase 1 UI components: `PersonAvatar` (`Scramble/Scramble/Components/PersonAvatar.swift`) — pure SwiftUI render component reading the active theme and `colorScheme` from the environment to resolve the person colour from `colorKey`. Three sizes via nested `PersonAvatar.Size` enum (compact 14pt, standard 26pt, large 36pt). Renders a circle with 16% colour fill, 1.5pt border at 33% opacity (inactive) or 100% (active, via `isActive` flag), and the name's first grapheme uppercased at full colour opacity in `.heavy` weight sized to ~42% of the diameter.
- `PhaseNodeMarker` (`Scramble/Scramble/Components/PhaseNodeMarker.swift`) with top-level `PhaseNodeState` enum (`past`, `current`, `future`) — past renders a filled circle in the phase colour with a centred white SF Symbol checkmark; current renders a filled circle in the phase colour (no glow ring per Decision 13); future renders a clear circle with a 1.5pt stroke in the phase colour. Configurable `diameter` with a 20pt default.

- Phase 1 theme system: `Theme` (id, displayName, dark/light `ThemeVariant`s, `PersonPalette`) and `ThemeVariant` (background `Gradient`, accent, surface, surfaceBorder, textPrimary, textSecondary, checkColour, warnColour, 7-entry `phaseColours`) value types under `Scramble/Scramble/Theme/Theme.swift`, plus `ThemeKey: EnvironmentKey` + `EnvironmentValues.theme` extension for SwiftUI environment injection. All theme types are `Sendable` and `nonisolated` so they cross task/actor boundaries safely.
- `PersonPalette` + `PaletteEntry` (`Theme/PersonPalette.swift`) with `entry(forKey:)` lookup and `nextUnusedKey(among:)` that always returns a non-optional entry (first canonical entry when the taken set is full, per design — the editor surfaces the duplicate-color advisory in that case).
- `MidnightAtlas` namespace (`Theme/MidnightAtlas.swift`) holding the dark and light variants per the UI design doc colour table, the 8-entry person palette (Cyan, Pink, Yellow, Green, Purple, Orange, Red, Teal) with hex values for both schemes, and the canonical `paletteKeys` order. `Theme.midnightAtlas` static instance wires it all together. Private `Color.hex6` / `Color.rgba` helpers are `nonisolated` for use in static initializers.
- Swift Testing suite `ThemeTests` (`ScrambleTests/Theme/ThemeTests.swift`) covering `variant(for:)` selection across dark/light, `personColor(key:in:)` resolution for every palette key in both schemes, unknown-key nil returns, `PersonPalette.entry(forKey:)` hits and case-sensitive misses, palette canonical order, and `nextUnusedKey` behaviour across empty / partial / sparse / full / unknown-key-tainted taken sets.
- Phase 1 persistence layer: SwiftData `@Model` entities `Trip`, `Person`, `MasterTaskItem`, `MasterPackingItem`, `TripTask`, `TripPackingItem` under `Scramble/Scramble/Models/`, all CloudKit-safe (default values on every property, no `@Attribute(.unique)`, client-assigned `id: UUID`).
- `SchemaV1` (VersionedSchema) and `AppMigrationPlan` (empty stages) in `Models/Schema.swift`, plus a shared `modelLogger` for persistence-layer logging.
- Inverse relationships with delete rules colocated on the owning side: `Trip.participants` (`.nullify`), `Trip.tasks` / `Trip.packingItems` (`.cascade`), `Person.tripPackingItems` / `Person.masterPackingItems` (`.deny`).
- Codable-bridge computed extensions on entities: `Trip.attributes`, `MasterTaskItem.conditions` / `phase`, `MasterPackingItem.conditions`, `TripTask.phase` / `source`, `TripPackingItem.state` / `source`. Decode failures log via `os.Logger` and fall back to defaults rather than crash.
- `Person.initial` computed extension returning the first grapheme uppercased (or `"?"` on empty), correct across simple letters, accented letters, and ZWJ-joined emoji.
- `EnvironmentProbe` value type (`Persistence/EnvironmentProbe.swift`) with injectable `environment` / `arguments` and a `production` factory reading `ProcessInfo.processInfo`. Branches: `isTest` (XCTestConfigurationFilePath), `isUITestHost` (`-uitest 1`), `isPreview` (XCODE_RUNNING_FOR_PREVIEWS=1).
- `ModelStore` (`Persistence/ModelStore.swift`) as `@MainActor enum`: `shared` evaluates `makeContainer(probe: .production)` once at first access; `configuration(probe:)` is `nonisolated` and unit-testable; CloudKit private container (`iCloud.me.nore.ig.scramble`) with `SchemaV1` + `AppMigrationPlan` in production; in-memory + `.none` in tests/UI tests/previews; silent local-only fallback on CloudKit throw logged with `[ModelStore.fallback]`; `fatalError` if local fallback also throws.
- Swift Testing suites for persistence: `SchemaTests` (container construction including with migration plan, six entity round-trips, inverse relationships in both directions, cascade rules, nullify on Person delete, `Person.initial` grapheme behaviour, Codable-blob and raw-enum bridges with unknown-value fallback, dangling `masterItemID` tolerance), and `ModelStoreEnvironmentTests` (each probe branch returns the expected `ModelConfiguration` shape; probe `isTest`/`isUITestHost`/`isPreview` flags; strict UI-test detection).
- `docs/agent-notes/persistence.md` capturing relationship layout, Codable-bridge contract, environment-detection branches, and gotchas (MainActor isolation, Schema construction, file-scope `nonisolated` for shared logger).
- `.swiftlint.yml` opting out of `inclusive_language` (the domain requires "master" terminology) and relaxing length/identifier rules for the schema test suite.

### Changed

- `specs/phase-1-foundation/decision_log.md`: added Decision 16 documenting that SwiftData iOS 26.4 does not reliably enforce `.deny` at save time on in-memory stores. The rule remains declared on `Person.tripPackingItems` and `Person.masterPackingItems` as defense-in-depth; primary enforcement is the UI guard in task 22 per requirement 9.7. SchemaTests replace runtime-throw assertions with inverse-traversal queryability tests.

- Phase 1 foundation types: `Phase`, `ItemSource`, `PackingState`, `TripAttribute` enums (`Models/Enums.swift`); `TripAttributes` Codable struct with `setSingle`/`toggle`/`selected` helpers and deterministic on-disk ordering (`Models/Codable/TripAttributes.swift`); `ItemConditions` indirect enum (`.always`/`.match`/`.all`/`.any`) with discriminator-keyed custom Codable and pure `evaluate(against:)` (`Models/Codable/ItemConditions.swift`); `TripStatus` enum (`upcoming`/`inProgress`/`returningSoon`/`completed`) with pure `compute(startDate:endDate:today:calendar:)` honouring calendar-day granularity and a 2-day `returningSoon` threshold, plus `LocalizedTripStatus` display formatter (`Features/Trips/TripStatus.swift`).
- Swift Testing suites covering all foundation types: `TripAttributesTests` (helpers + Codable round-trip property test across 240 generated samples + corrupt-blob fallback), `ItemConditionsTests` (evaluator table including vacuous empty `.all`/`.any` + idempotence + distributive equivalence + depth-bounded round-trip), `TripStatusTests` (every status case + midnight/timezone boundaries).

### Changed

- `ScrambleApp.sharedModelContainer`: switched the template scaffold to an in-memory store under XCTest and added a local-only fallback on container construction failure so tests can run before `ModelStore` (task 11) replaces this code path.

- Xcode project (`Scramble/Scramble.xcodeproj`) generated from the iOS App template: SwiftUI + SwiftData, iOS 26.4 deployment target, Swift 6 language mode, MainActor-default actor isolation, approachable concurrency.
- CloudKit capability wired to container `iCloud.me.nore.ig.scramble` (entitlements + `UIBackgroundModes: remote-notification` for silent push).
- `Makefile` wrapping `xcodebuild` with help/build/test/install/run/lint/format targets, conditional `xcbeautify`/`swiftlint`/`swift-format` detection, repo-local `./DerivedData`, and serial simulator execution to avoid flakes. Defaults: `SIMULATOR=iPhone 17 Pro`, `DEVICE_MODEL=iPhone 17 Pro`, `CONFIG=Debug`.
- `.gitignore` covering Xcode user state, build output, SPM, and common third-party tool directories.
- Functional design document (`docs/scramble-design-doc.md`) covering trips, phases, tasks, packing lists, the rules engine, CloudKit sharing, and the conceptual data model.
- UI/UX design document (`docs/scramble-ui-design-doc.md`) covering the timeline-based Trip Detail screen, packing sheet (pack/repack modes), explainability long-press, Midnight Atlas theme, person colour palette, colour semantics, haptics, and accessibility.
- React/HTML visual prototype (`docs/data.jsx`, `docs/direction-a.jsx`, `docs/Scramble Directions.html`) as reference for the intended look-and-feel — not a port target.
- `CLAUDE.md` orienting future Claude Code sessions to the design docs, load-bearing architectural decisions, the recommended implementation order, and the Makefile-driven build/test workflow.
- Phase 1 foundation spec under `specs/phase-1-foundation/`: requirements (56 acceptance criteria across data model, CloudKit container, theme, app shell, Trip List + auto-open, Trip Detail scaffold, Master Lists scaffold, Trip editor, people management, avatars), design (architecture, components, data model with SwiftData entities + inverse relationships + delete rules + Codable blobs + VersionedSchema, error handling, testing strategy), decision log (15 Nygard ADRs covering scope cut, global people, date semantics, theme/appearance, country-flag deferral, tab-bar hide on detail, no seeded people, CKShare-zone deferral, data-model integrity, VersionedSchema-from-day-one, behavioural test/preview rule, person duplicates, minimal phase-node states, string-raw enum bridges, cross-device person-delete tolerance), and 27-task implementation plan with TDD pairing and stable-ID dependencies (`tasks.md`).
